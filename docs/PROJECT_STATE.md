# Project state

**Purpose.** This is the handoff file between parallel Claude Code sessions and between Claude Code and Claude Chat. It holds what neither the code nor the git history tells you: what is live versus staged, what is blocked and on whom, rulings the founder has made, and what comes next. It is not a changelog. If something here contradicts the code, the code is right and this file is stale, so fix it.

**Last updated:** 2026-08-12 (commerce Phase A closed at the real edge; Merchant API unlocked and swept; HoI client report drafted and corrected; instawarm $225 engagement agreed; SingularCommerce strategy verdict delivered; cross-surface relevance check adopted as method)

**If you are a fresh session, read §1 and §2 always.** Then read only your track: §4 for lead generation, §5 for ecommerce. §3 (the named agents) is vertical-agnostic and belongs to both.

**Two Supabase projects exist and they are easy to confuse.** The portal DB (`SUPABASE_URL` in `.env.local`) holds everything the Next.js app reads. The substrate DB (`SUBSTRATE_DB_URL`) holds what n8n reads. `docs/substrate-migrations/` contains migrations for both despite its name, and on 2026-07-30 a rename meant for the portal was pointed at the substrate. Read `docs/substrate-migrations/README.md` before running any migration.

**The agents are named. Rexos is not one of them.** Bernard owns Meta (§3). Oscar owns Google Ads and Shopping, renamed from the misleading "Ask Rexos" on 2026-07-30 because the platform's name was standing in for a single-channel specialist. Rexos is the platform they both live inside.

---

## 1. Entities, and which one you are acting for

Getting this wrong has already cost credibility once, so check before drafting anything client-facing.

| Entity | Registration | Used for |
|---|---|---|
| **SingularWeb** | (holding) | Owns the infrastructure and Rexos itself |
| **Web Marketing International Ltd** | UK, CRN 10264568, active since 6 July 2016, registered 124 City Road, London EC1V 2NX | The UK agency. Owns the WMI UK ad account. The entity for UK Twilio numbers. |
| **ADENERGY SP. Z O.O.** | Poland, KRS 0001251633, Tyniecka 137T, 30-319 Kraków | Legal entity behind DentalMastery.ai. Makes that operation GDPR-governed with UODO as supervisory authority. |
| **WEB MARKETING INTERNATIONAL - FZCO** | UAE, Dubai trade licence 26411 (Dubai Integrated Economic Zones Authority), licensed 15 Feb 2023, registered DSO-IFZA, IFZA Properties, Dubai Silicon Oasis | The Dubai agency entity. instawarm.shop's $350/month retainer is signed through its portal. The register also names it for the UAE TikTok acquisition lane. |

The founder is Anthony, resident in Dubai. **His location is an operational fact and almost never a regulatory one.** WMI is a UK company with a UK registered office; do not treat it as foreign, and do not manufacture compliance doubt from where he sits.

The "Mastery" brands are outside SingularWeb. See the memory note on the brand and entity map.

---

## 2. Standing rulings and conventions

These are founder rulings, not preferences to weigh. They apply to both tracks.

**Meta is read-only for Claude.** Ruled 2026-07-24 after a denied script. Every Graph API call Claude makes is a GET. Never offer to do a Meta write directly; the write paths are below.

**Meta writes have two paths. Manus is cancelled.** In order of preference:

1. **The substrate** (`BERNARD_build` for campaign construction, `BERNARD_fix` for a founder-approved single mutation). Deterministic, zero executor credits, no false-claim risk. This is now the only automated path.
2. **The founder by hand** in Ads Manager, for anything UI-only.

**Manus: cancelled, founder-confirmed 2026-07-30.** Do not design anything around it. Note the capability this loses: Manus could browse, drive a UI and improvise around an undocumented obstacle, and `BERNARD_build` cannot. A task genuinely needing those is now a human-tier item for the founder rather than an executor task, and should be named as such in `actions_not_taken`.

*Reconciling the API evidence, since a parallel session found it and it looks contradictory.* The `Manus usage` node in `BERNARD_monitor` returned 200 with live data on the morning of 2026-07-30, including a 3,068 credit grant labelled "Upgrade plan" timestamped 2026-07-24T21:33Z. That is consistent rather than conflicting: the founder said "I have upgraded" on 24 July, which is that grant, and the cancellation came after. A cancelled subscription normally keeps answering until the paid period lapses, so a 200 from the usage endpoint is not evidence of an active subscription. **The founder's statement governs.** The `Manus usage` node was already removed from `BERNARD_monitor` on 2026-07-30 as part of the gate fix, so nothing polls that endpoint any more; the remaining Manus surface is `RCV_manus_events`, covered by the canon note below.

Whichever path, **everything is created PAUSED and only the founder activates or touches billing.** Any executor report is still read back against ground truth. That rule outlived its original reason (Manus produced two verified false claims) and now guards our own bugs, partial failures and races against founder edits made in Ads Manager. Claimed is still not true until read.

Bernard's role is therefore spec-writer, verifier and gatekeeper, with the substrate as executor.

**Canon updated 2026-07-30.** `~/Downloads/WMI_META_LAB_EXECUTOR_CONTRACT_v2.md` supersedes v1: transport is substrate-internal, the Manus skill doctrine-carriage mechanism is void because doctrine is now code rather than instructions, and four controls upgrade from declared-and-audited to machine-enforced (report shape, write budget, gate hold, stand-down). `RCV_manus_events` was **retired 2026-07-31 on founder instruction**: both copies deactivated and renamed `_ARCHIVED_RCV_manus_events_v2` / `_v1-old`, JSON backups in `~/Downloads/n8n-archive/`. Nothing listens for Manus any more.

**BERNARD_build hardened 2026-07-31, after its record was actually read: 2 built, 1 built_with_problems, 6 build_failed across 9 lifetime attempts.** Three changes, live in n8n and verified. (1) Gate hold is now pre-flight: any envelope `gate_conditions` entry not explicitly `met` returns GATE_BLOCKED before any Meta call; proven with a deliberately blocked live envelope, zero Meta writes confirmed by read-back, rejection logged. (2) Error capture rebuilt: Graph returns a NULL element for ops whose dependency failed (previously stored as the useless `code null {}`), Meta's actionable text lives in `error_user_msg` and `error_data` blame fields (previously dropped, leaving bare "Invalid parameter"), and a top-level batch failure returns an object not an array (previously smeared across op slots). All three shapes unit-tested. (3) Write-budget pre-flight turned out to already exist; the contract had under-credited it. Failures are now diagnosable from the task row alone.

**R15.1 ADOPTED by the founder 2026-08-08. No founder rulings are open.** `~/Downloads/R15_AMENDMENT_2026-07-30.md`, applied to `~/Downloads/CODE_BRIEF_WMI_META_LAB.md` §2 and `~/Downloads/WMI_META_LAB_EXECUTOR_CONTRACT_v2.md` §8 (blocking input 2 now CLOSED). The quarantine attaches to the **capability**, not to the executor's identity, so it survived the move from Manus to `BERNARD_build` and would survive any future change of executor. **Graduation bar, now written rather than felt: ten builds executed and verified with no `verification_failed`, across at least three clients, with at least one live campaign the founder is willing to show a client.** Adopted from the amendment's suggested figures; the founder can move the numbers. Bernard's memory holds a decision entry on the write paths that still needs revising to match. Historical note on what it replaced: R15's quarantine language ("an isolated, swappable execution substrate") described a real vendor boundary that no longer exists now the executor is our own infrastructure. R15.1 is drafted to make the quarantine attach to the capability rather than the executor's identity, and to keep it until evidence-proven. One word adopts.

**Client documents are written as Anthony**, first person, in his voice. Never mention APIs, tooling, endpoints, field names or how data was obtained. He is selling his expertise, not a generated report. **First person means singular, ruled 2026-08-03: I, me, my, never the agency "we/us/our"**, even where it feels natural ("we cut the videos", "that work is on us"). Applies to freelancer-facing writing too. Sweep every draft for "we" before delivering.

**WhatsApp bridge positioning, ruled 2026-08-07.** Offered free where it serves retention, and the reason is never stated to the client. Ownership is deliberately generous: the client owns the number, the WhatsApp Business Account (with us as partner) and the Google Ads account, and keeps all of it plus a data export on exit. That is both the honest position and the strategically strong one, because it also moves client assets OUT of our over-concentrated Meta portfolio, which is the highest-ranked risk in the operation. **The retention mechanism is the loop, not the code.** The widget is copyable in a day; what is not copyable is the accumulated offline-conversion history feeding Smart Bidding, which regresses toward click-optimisation within weeks if the feed stops, plus the attribution history a replacement supplier cannot backfill. The line we do not cross: their data export stays easy and cheerful on exit. **Free infrastructure retains nobody unless it is visible**, so every bridge client gets a short monthly attribution note (`docs/WA_MONTHLY_NOTE_TEMPLATE.md`); without it the moat protects nothing because nobody notices it.

**WhatsApp bridge claims register, ruled 2026-08-06.** Approved: click captured server-side before the visitor leaves; leads created automatically in the CRM with source attached; attribution method labelled per lead (exact / window / unattributed) so coverage is measurable, not claimed; refuses to guess under ambiguity, degrades to unknown never to wrong; completed bookings feed back to Google and Meta. **Banned: "a code they cannot delete"; "invisible tracking"; any unqualified accuracy percentage; any claim of attributing direct or saved-number messages.** Mandatory in commercial documents: accuracy is concurrency-dependent and strongest for low-simultaneous-enquiry businesses. The 30-minute window is a per-client tunable default, not a finding. Full context: `docs/WA_BRIDGE_CORRECTION.md`. This is also the first instance of the claims-gate discipline the strategy surface asked for; the pattern generalises to any product claim.

**No em dashes. Anywhere.** Objected to three times. Use full stops, commas, colons, parentheses. En dashes only in numeric ranges. Check headings, which is where they survive a prose pass.

**Never assert something is absent from a single API reading.** On 2026-07-30 an Instagram post count of zero became a client document's headline finding and was wrong; the profile was full. Before any absence claim, check the surface a real user would see. A permission error nearby disqualifies the whole area from absence claims.

**Verify who controls a system before drafting anything about it.** A DNS request went to the wrong provider on 2026-07-30 because they were in an email thread. Check the system itself. Never speculate in client-facing writing about whether the founder has access to something; ask him.

**R7 autonomy.** Self-initiated work is fine when it is additive, reversible, verified, touches no client-facing surface and writes no production rows. Modifying a live workflow needs a founder ask.

**Rexos `main` is production.** It auto-deploys to app.wmiltd.com. Use a preview branch unless the founder says deploy.

---

## 3. The named agents (vertical-agnostic, shared by both tracks)

Bernard is the **senior paid social strategist and media buyer**, ruled 2026-07-30. Auditing and campaign building are things he does, not what he is. He is intrinsically neither ecommerce nor lead generation, so both tracks use him and both should keep his memory current.

**Where he lives.** Chat at `/bernard` in the portal (`src/components/BernardChat.tsx`, `src/app/api/bernard/chat/route.ts`, `src/lib/integrations/anthropic/bernard-agent.ts`). Claude Fable 5, medium effort, server-side fallback to Opus 4.8. His governed n8n endpoints are in `src/lib/bernard.ts`.

**Tools.** `get_status`, `decide_fix`, `stand_down`, `list_meta_accounts`, `run_audit`, plus `remember`, `revise_memory`, `forget`.

**Memory (live as of 2026-07-30).** Table `agent_memory` (was `bernard_memory`; generalised in migration 0003 so Oscar shares it, scoped by an `agent` column). Migrations 0002 and 0003 both applied, **both against the PORTAL database, not the substrate**, see `docs/substrate-migrations/README.md`. Deliberately separate from `agent_conversations` so clearing the chat drops the transcript and leaves what he knows intact. Read fresh each turn and injected into his system prompt grouped by subject with ids. Seeded with 20 foundational memories. Capped at 150 rows / 24k chars, at which point he is told to consolidate.

**Attachments (live as of 2026-07-30).** PDF goes to Claude as a native document block; .docx text is extracted from the OOXML via jszip; .md, .txt and .csv decode directly. Legacy .doc is refused with a message rather than half-parsed. Limits are 5 files, 3.5MB each, 4MB combined, set by Vercel's body ceiling. Extracted text persists into the transcript; **PDF bytes do not**, so a long PDF thread needs re-attaching.

**Substrate workflows.** `BERNARD_dispatch`, `BERNARD_fix`, `BERNARD_monitor` (daily 09:00), `BERNARD_standdown`, `BERNARD_build`. `RCV_manus_events` retired 2026-07-31 (deactivated, renamed `_ARCHIVED_`, backed up); its raw-body signature verification lives on as the pattern inside the commerce receiver's `verify_shopify_hmac`.

**Ad accounts he can currently see** (system user, read-only):

| Account | Business | Track |
|---|---|---|
| act_1027063116856202 WMI UK | WMI | lead gen (DM.ai runs here) |
| act_1766396370547849 Steffen Foerster | Steffen Foerster | lead gen (high ticket) |
| act_575423175548816 Tropical Oasis | Ace Nutrition | ecommerce |
| act_1801857321221826 Luca Summer | Atelier Brunos | ecommerce |
| act_27875735492115545 Mondedutabouret | monde_du_tabouret | ecommerce |
| act_799323799456382 instawarm.shop | instawarm.shop (Vincent) | ecommerce |
| act_1304153623765783 FR | Vasco Electronics (Vasco Translator France) | ecommerce, largest account: 833,726 PLN lifetime |

### The agents-and-contractors operating model, ruled 2026-08-18 (supersedes the collision note that briefly stood here)

**Human contractors stay. Both Gopoxy and Steffen are freelancer-managed, and the founder is not parting with his human contractors.** What changes: **Bernard (Meta) and Oscar (Google) now hold build and optimise capabilities, exercised only in founder-triggered sessions**: the founder decides, triggers an audit, then a build and/or optimisation session follows. The purpose is to **complement, improve or correct the freelancer's work**, never to replace it or race it. **Norbert supervises both agents**: every build or optimise session passes his review before the founder sees it. This resolves the apparent 2026-08-13/2026-08-18 Gopoxy conflict: watch-and-flag remains the standing posture between sessions, and a founder trigger is the only thing that opens a write window.

Consequence for anyone building or running the optimise executor: on a freelancer-managed account the pre-flight must read the platform's own change history, not just `move_snapshots`, because the thrash gate has to count the freelancer's changes too, and **no agent move may reverse a recent human change without that fact being stated in the approval item**. Spec: `docs/BERNARD_OPTIMISE_SPEC.md`; seven prior rulings recorded there.

**Meta leg BUILT and smoke-tested 2026-08-18.** Live in n8n: `BERNARD_optimise` (tgmHGKPDNy78Ozkd) and `BERNARD_optimise_execute` (ywJvDCQfDSPJYdbQ), both behind the dispatch key. Migration 0008 (optimise_sessions, optimise_moves) applied; substrate rows created for `steffen-foerster` (act_1766396370547849) and `gopoxy` (act_282111861642935), both doorway `wmi_meta_lab`, ceiling 3, thrash 4, budget bound ±25%. Gates unit-tested 24/24 against the deployed code (`tests/optimise-gates.test.js`) and proven live: allow-list refusal logged as a counterfactual, full chain staged a deliberately inert proposal on Steffen, Norbert (Opus, metered: first run $0.03) flagged the smoke test as a no-op in Q1 **and found a real account finding in Q2** (the August campaign paused with heavy Meta-system churn over Antoine's manual edits), the human-change check named Antoine's own 17 Aug edits, the move was rejected cleanly and a double-decide refused. **Zero Meta writes have ever been made; the write path (approve leg) is built and untested by design, and the first real approval should be a founder-chosen low-stakes move.** Graduation count: 0 of 50.

**Oscar's leg (spec §9) BUILT 2026-09-03, portal-side.** `src/lib/norbert-review.ts` runs Norbert (claude-fable-5, metered under `norbert`, scope `review:oscar:<id>`) on every proposal the moment Oscar files it, and on demand from the card (Ask Norbert). Evidence: the target campaign's seven-day state and its `change_event` rows with `user_email`. Rules in `norbert-review-rules.ts` (8 tests): thrash at 4 in 7 days, human change named for any login outside `GOOGLE_ADS_OWN_LOGINS` (unset today, so every login is named), unreadable history fails closed, one revision round via `details.revision_of`. Gates in `decideProposal` and the `applyProposal` worker refuse an unreviewed proposal and admit an objected or review-failed one. Return leg to Oscar's inbox on any non-SOUND outcome. Migration 0026 (`norbert_review`, `norbert_reviewed_at`): applied to FZCO by the runner; **wmiltd needs the founder's SQL editor, and until it runs Approve on app.wmiltd.com fails with a column error.** **First three live reviews ran 2026-09-03 05:05 UTC on the Fly-Rides negatives** (founder applied 0026 on wmiltd, reviews triggered from Code): all three SOUND, two flagged thrashing (4 changes in 7 days) with the human changes named as booking@fly-rides.com and Recommendations auto-apply; Q2 on all three named the untouched root cause (the auto-apply subscription that strips client negatives roughly every 33 hours) and one noted a second deleted negative not being restored. Cost $0.18 for the three, metered. Two inbox notes reached Oscar. Q2 findings handed to the Fly-Rides session via the founder.

**Glue-level smoke execution BUILT 2026-09-03 and it found two live bugs.** `tests/optimise-glue.smoke.js` runs both optimise workflows' Code, If and Switch nodes for real (n8n's `$input`/`$('Node')`/`$json` emulated, every `{{ }}` template resolved and checked for `undefined`/`NaN`), fed by external-node outputs captured from six real executions (`tests/fixtures/optimise-glue.json`, paging stripped because Graph echoes the token in `paging.next`). 14 scenarios: the three real staging runs, the 26 Aug crash replayed past the crash point, the rejection, the already-excluded abort, plus synthetic pause/budget, unreadable history, unparsable Norbert, mismatched read-backs and the ceiling race. `--db` runs the Postgres nodes' real SQL against the substrate inside a transaction that always rolls back; `--deployed` diffs n8n against the generator. **Bug 1:** the verifier's strict diff would have marked every real exclusion `verification_failed`, because Meta appends `targeting_relaxation_types {lookalike:0, custom_audience:0}` on any targeting write; the 26 Aug read-back proves it. Fixed: that all-zero default appearing where the snapshot had nothing is not a change; any non-zero relaxation still fails. **Bug 2:** both verifiers interpolated `created_at`, which the Postgres node hands over as a JS Date, into a `timestamptz` literal; Postgres rejects the `GMT+0000 (...)` string, so the first real approve would have written to Meta and then failed to record it. Fixed with an explicit ISO conversion. Neither path had ever completed in production: the four 26 Aug moves were reconciled by hand. Both workflows redeployed from the generator by `scripts/deploy-optimise-workflows.py`; drift check clean; `npm test` now runs every suite.

**Full n8n and Supabase audit 2026-09-03, founder-instructed.** Record in `docs/N8N_SUPABASE_AUDIT_2026-09-03.md`. Fixed live and verified: error handler on 15 active workflows that had none (the daily backup among them, which is why its 12 August failures went unnoticed); `x-rag-key` header auth on the four internal webhooks (agent core, post-pass, knowledge QA, router) with the five internal callers armed, unauthenticated calls now 403; agent core tolerant of a null tenant id and the webchat wrapper degrading to the unavailable line (live probe green); MAINT_conversations_doc moved to :20 to stop fighting kb_ingest for Drive quota; two temp probe workflows deactivated. **Backups: green daily at 20:00 UTC since 16 Aug, 55 workflows to antoinem78/n8n-backups; credentials are not backed up.** **Meta token moved to header auth 10:15 UTC** (credential `5cOSHv4rFTASt7iF`, 21 nodes in 8 workflows, proven by a live read; generator updated so redeploys keep it; BERNARD_build's duplicate node ids repaired on the way). Pre-swap executions holding the token pruned (8, all BERNARD_optimise; instance-wide re-scan clean). RCV_form_routing deactivated as dead. **Remaining follow-ups PARKED by the founder 2026-09-03 as minor:** hardcoded GHL PIT in RCV_form_routing, Shallowford eConnector down since 5 Aug, GHL webhook headers, Slack signature, Barsoum legacy workflow, eight unused credentials.

### The Meta audit standard, reset 2026-08-11

**Founder ruling: Bernard's Meta audits were not good enough.** The benchmark is the three manually produced client decks (Befitnow.ca, Harvest & Herd, GetUpSpace): a deck rather than a Word document, with cover, mission statement, one finding per screenshot, funnel analysis metric by metric against benchmarks, a strategy section of campaign proposals, and a creative requirements slide. Ad account screenshots sit beside the findings.

**The architectural fix.** The old generator handed raw account JSON to the model and asked for 1,200-1,800 words, which produces plausible generic prose and misses the specific quantified things that cost money. It also had no placement, age, country, audience, exclusion, funnel or creative data at all. Now:

- `src/lib/integrations/meta/audit-deep.ts` does the deep read (breakdowns, audience library, exclusion coverage, ad-level performance, creative variation, monthly trend).
- `src/lib/audit/meta-findings.ts` holds 14 deterministic detectors. Each finding carries its own evidence and, where computable, its money at stake per 30 days. Two rules: **a section that failed to read is skipped, never reported as an absence** (a permission error on one edge is not evidence a thing is missing), and every finding prices itself where it can.
- `src/lib/audit/meta-generate.ts` feeds the *verified findings* to the model, which writes them up in order and may not add findings or supply a figure of its own. A "What Is Already Right" section stops the audit reading as a hit list.
- `node scripts/meta-audit-findings.mjs <account-id> [days]` prints what an account raises, without generating a document.

**Do not port the template decks' findings to a new account.** All three templates were "you have not built X" accounts. Vasco FR is the opposite: 105 audiences, 20 lookalikes, advanced matching already on. Writing "no lookalikes, advanced matching off" there would have been false. Run the detectors and write what they raise.

### Oscar (Google Ads and Shopping), live 2026-07-30

**Renamed from "Ask Rexos" on 2026-07-30.** The platform's name was attached to a single-channel specialist: every one of that agent's eight tools is Google Ads (search terms, Merchant Center feed audits, GAQL reads). Founder ruling: the agent is Oscar, Rexos means the platform, and there is no concierge layer because there would be nothing left in it. **Do not call him Rexos in code, UI or writing.**

**Where he lives.** The floating "Ask Oscar" launcher on every admin page (`src/components/RexosWidget.tsx`, still the filename) plus the panel in `src/components/CommandChat.tsx`. Brain is `src/lib/integrations/anthropic/agent.ts`, endpoint `/api/agent/chat`, Claude Opus 4.8.

**Tools.** `list_accounts`, `list_campaigns`, `get_account_report`, `get_all_account_summaries`, `get_recent_changes`, `get_search_terms`, `get_feed_audit`, `propose_optimization`, plus `remember`, `revise_memory`, `forget`. He cannot execute: `propose_optimization` files a reviewable card and the founder approves.

**Persona.** Senior paid search strategist, same seniority framing as Bernard. Owns Google Ads and Shopping across every client, expected to hold a view and defend it.

**Memory: shared table, isolated per agent.** He has zero memories as of 2026-07-30 and will accrue them. Worth seeding him deliberately the way Bernard was, since an empty memory is the slowest way to start.

**Conversation scope is still `command-center`.** Deliberate: renaming the scope would orphan the existing history. Only the persona and the UI strings changed.

**Audit document, live 2026-07-30.** `run_audit` hands the founder a link to the existing Google Ads Audit and Growth Research .docx. Worth knowing that the generator was already there (`src/lib/audit/generate.ts`, with account-type detection, a severity-graded diagnosis pass and a forecast) and already reachable from the client page via `GenerateAuditButton`. **The only gap was that it could not be reached from a conversation**, so nothing was rebuilt: `/api/audit/[clientId]` gained a GET handler alongside its POST (a link is a GET; the button still POSTs, both share one implementation), and Oscar's panel gained link rendering and a download chip, which it had never had.

Constraint to know: the audit is keyed by **client id**, so it only covers imported clients. A bare MCC account visible under the roster has no client record to attach one to, and Oscar is told to say so plainly rather than invent an id.

### Chat-native execution, live 2026-08-01

Both agents now execute from the portal chat, on the founder's explicit word, behind unchanged machine gates. **Bernard: `dispatch_build`** sends an agreed spec to `BERNARD_build` (entities PAUSED, pre-flight gate holds, write budget, allow-list, build_ref idempotency, read-back-verified report). **Oscar: `list_proposals`, `decide_proposal`, `apply_proposal`, `dry_run_proposal`**, calling the exact functions behind the Proposals page buttons; `applyProposal` re-checks approval and every guardrail before any mutate. **GOOGLE_build live 2026-08-01**: Oscar builds full Search campaigns from chat via `dispatch_build` (`src/lib/integrations/google-ads/build.ts`). It lives in the portal because that is where the Google credentials live, but holds the executor contract: campaign always created PAUSED, gates in code (kill switch, customer allowlist, budget hard cap, op budget, RSA limits, duplicate-name refusal), atomic single-mutate build (partialFailure false, so all-or-nothing), verified by re-read of status and entity counts, every attempt in `write_audit`. `validate_only` is the founder-facing dry run through Google's own validator. Search only; PMax, Demand Gen and Shopping are named not-buildable. Operation shapes proven against the live v24 validator (8/8 accepted, nothing created). **Production-proven 2026-08-01: Oscar ran a validate_only build for VIP through the deployed portal and Google validated it.** So VIP (6719680160) is buildable in production: the deployed env passes `guardAllowlist` for it (either `ALLOW_ALL_MCC_ACCOUNTS=true` or VIP is on the list; which switch is set was not read directly, only proven behaviourally). The local .env.local allowlist differs from production; trust the deployed behaviour, not the local file.

### Shared memory infrastructure

Table **`agent_memory`** in the **portal** database, migration 0003, applied and verified 2026-07-30. One row per memory, scoped by an `agent` column (`bernard`, `oscar`). Scoped on reads and writes both, so neither agent can revise or forget the other's memories: a conclusion about Meta delivery rarely transfers to a search auction. Deliberately separate from `agent_conversations`, so clearing a chat drops the transcript and leaves what the agent knows intact.

Verified at merge: 20 memories, all attributed to bernard, isolation confirmed by attempting a cross-agent update and getting a zero row count.

**Shared pool LIVE 2026-08-01 (migration 0004 applied).** 13 memories re-flagged shared (every client-kind memory from both agents, the DM offer strategy, the KST OCT unblock, the OCT-pipeline-distrust fact); both agents see 31 memories. Note the agents have been writing memories organically since the tools shipped. `shared boolean` on `agent_memory`: ownership and edit rights stay with the writing agent; `shared=true` makes a memory visible to all agents with provenance shown ("SHARED by bernard"). Prompts teach the split: client-level facts, founder rulings and cross-channel strategy shared; platform tactics private. The loader falls back to per-agent queries until the migration runs, so the code is already live safely.

**If you add a third agent**, it needs a name that is not "Rexos", an `AGENT` constant, and its memories are free: pass its name to the same four functions in `src/lib/agent-memory.ts`.

### Agent relay: one thread across surfaces (Bernard live in production 2026-08-03, Oscar built, key pending)

The founder speaks to Bernard and Oscar from Claude Code sessions, not only the portal, and each agent's two surfaces are one conversation ("like Slack and email with the same contractor"). Mechanics: `/api/bernard/chat` and `/api/agent/chat` accept a relay header (`x-bernard-relay-key` / `x-oscar-relay-key`, env `BERNARD_RELAY_KEY` / `OSCAR_RELAY_KEY`, timing-safe compare) as an alternative to the Auth0 admin session. Client: `scripts/agent-relay.mjs <bernard|oscar> "msg"` (Oscar also takes `--scope <client-uuid>` for per-client threads; `bernard-relay.mjs` is a compat wrapper). Repo skills `/bernard` and `/oscar` teach any Code session to relay verbatim, never impersonate. Relay turns carry actor `antoine.martin@wmiltd.com (code chat)` for provenance. DELETE (clear thread) is deliberately portal-only on both routes. **Bernard: production round trip verified 2026-08-03.** **Oscar: proven locally 2026-08-03; needs `OSCAR_RELAY_KEY` in Vercel** (value in `.env.local`, never the repo) then a redeploy. Keys are per-agent so either can be revoked alone.

**The relay is per-machine, not per-workspace (learned 2026-08-03).** The scripts and skills travel with the repo, but the keys do not: a Code session on another device fails with "RELAY_KEY is not set" until that machine's repo is pulled to at least `bb4a709` and `BERNARD_RELAY_KEY` / `OSCAR_RELAY_KEY` are copied into its `.env.local` by hand. The founder hit exactly this on a second Mac; if he reports an agent "can't hear" him from Code, check those two things before debugging anything deeper.

---

### app.wmiltd.com mirror status, audited 2026-08-05

Code and schema already match (one repo, and `clients.currency` / `platform_client` / `agent_memory` all verified present on the wmiltd portal DB). The gaps are configuration and provider-specific:

1. **Email silence, now explained.** wmiltd sends nothing at any stage. Two independent causes: Resend is unset (`RESEND_API_KEY` / `EMAIL_FROM`), AND **every PandaDoc send sets `silent: true`** (deliberate: the portal embeds the document), so PandaDoc emails nobody either, contrary to the natural assumption that it emails both signers. Fix half of it with a Resend domain for wmiltd.com (DNS at **Bluehost**), and the other half is shipped: the PandaDoc webhook now downloads the completed PDF and emails it to the client and to `CONTRACT_COPY_TO`.
2. **Stripe: unresolved and the highest risk on that portal.** The local key is `sk_test_`, and querying with it shows exactly one webhook endpoint, `https://app.wmiltd.com/api/webhooks/stripe`, correct events, but **`livemode: false`**. If production runs a live key against a test-only endpoint, a real client pays and never activates. Founder must confirm in the Stripe dashboard, Live mode, that an endpoint exists for that URL with the four events, and that its signing secret is the one in `STRIPE_WEBHOOK_SECRET`.
3. **Agreement two-party parity is a PandaDoc template edit**, not env: `AGREEMENT_SIGNATORY_*`, `AGREEMENT_GOVERNING_LAW` and `ENTITY_REGISTRATION_INFO` do nothing on that path because PandaDoc renders its own template. Add the Provider execution block inside the template. `CONTRACT_COPY_TO` IS worth setting: it drives the issue notice and the new signed-PDF copy.
4. **Defect found and fixed while auditing:** the issue notice was emailing the founder a PandaDoc *signing session* (single-use, one hour, scoped to the client's recipient identity). Each provider now supplies `internalDocumentUrl`: PandaDoc's app document view under his own login, the engine's permanent URL.
5. **SUPERSEDED the same afternoon, recorded only on 2026-09-03.** This paragraph was committed at 12:25 UTC on 2026-08-06; the rehearsal ran at 12:51 to 12:57 UTC that day on test client WE BET and was never written back here. The live activity log read at 21:01 UTC showed the full chain: client_created, onboarding_invite_emailed (12:51:56), details_confirmed, contract_generated, contract_signed (12:54:01), checkout_started, payment_completed and client_activated (12:57:56), with a £1 live Stripe charge the founder was then asked to refund. Those activity rows were cascade-deleted with the WE BET client on 2026-09-03, so today's log is silent and the transcript of the 6 August session is the surviving evidence. The invite email leg is evidenced as an event, not as a delivery; re-check delivery before the first real wmiltd client. Twenty-eight days of a wrong sentence in this file is the lapse the same-commit rule exists to prevent.

### Agreements are two-party as of 2026-08-05 (founder flagged: he never signed and never got a copy)

Three gaps closed, all env-gated so unset deployments are unchanged. (1) The agreement's intro now carries a Provider execution line ("Executed for and on behalf of the Provider by NAME, TITLE on DATE. The Provider is bound from the date of issue; the Client's acceptance below completes it"), from `AGREEMENT_SIGNATORY_NAME` / `AGREEMENT_SIGNATORY_TITLE`. Click-wrap was already binding without it; the point is that a one-signature document reads lopsided and left the founder without a counter-signed record. (2) `sendContractIssuedNotice` emails him the executed copy the moment the client generates it, closing the real hole: the document generates on the CLIENT's action, so he never saw it before they did. (3) The signed-copy email now also goes to `CONTRACT_COPY_TO`. **Verified 2026-08-05 by rendering the engine's own template against the portal's exact payload: the execution line and the client acceptance stamp both appear on the signed document.** Still unverified: the deployed env values themselves (a typo would show as a missing name), which the next real or throwaway generation proves. Proposal-engine path only; the wmiltd PandaDoc path would need a template change. Atelier Brunos's existing agreement predates this and stands as binding; the founder sends Gibran the document link with a line confirming FZCO's execution rather than re-signing a paying client.

### FZCO entity clone: app.webmarketinginternational.com (added to this file 2026-08-04)

A full second deployment of this same repo for Web Marketing International FZCO (USD): own Vercel project, own Supabase, own Auth0 tenant, own Stripe account, and CONTRACT_PROVIDER=proposal-engine (the self-hosted click-wrap engine at proposals.webmarketinginternational.com) instead of PandaDoc. Provisioned around 2026-07-09 per `docs/FZCO_CLONE_CHECKLIST.md`; it predates this file and was never recorded here. Verified from outside 2026-08-04: portal live, auth chain correctly wired to its own tenant, engine up and enforcing auth, Stripe and proposal-engine webhook routes deployed. **E2E dry run PASSED 2026-08-04, founder-driven: all six steps** (admin login, client creation in USD, share link render, engine proposal generate + click-wrap accept, Stripe TEST checkout, webhook-driven paid state). The clone is armed for rehearsal. **Still on Stripe sandbox: switching to LIVE keys and re-registering the live webhook secret are founder-only steps before any real client pays.** Gap found during the run and fixed same day: the portal had NO email sending at all (PandaDoc used to do it as a service; the engine returns links). Now `src/lib/email.ts` (Resend, env-gated): onboarding invite on client creation + payment confirmation from the Stripe webhook. LIVE AND PRODUCTION-READY 2026-08-04 late evening, proven on live rails end to end: Stripe LIVE keys + live-mode webhook (per-mode endpoints, the classic trap, handled), $1 live payment completed and client_activated by the live webhook one second later, invite + welcome + signed-agreement-copy emails all delivered (the contract-copy email was a gap found during the live test and built the same hour: proposal-engine webhook emails the permanent document URL on click-wrap acceptance). Governing-law defect found by the founder (default "England and Wales" on a UAE FZCO) and fixed via AGREEMENT_GOVERNING_LAW + ENTITY_REGISTRATION_INFO + VAT vars in the FZCO env, verified on a throwaway agreement. All test clients and rows deleted; client list starts clean. **FIRST REAL CLIENT PAID AND ACTIVE 2026-08-05: Atelier Brunos (Gibran Zaki), Managed Meta Ads Service, AED 1,800/month.** Invite 07:41 UTC, details 08:10, signed 08:11 (correctly titled Managed Meta Ads Services Agreement, wording fix deployed 20 minutes ahead of his click), checkout 08:11, PAID 08:13, active 08:13, Meta access task done 08:18. Invite-to-paid in 32 minutes. **atelier-brunos registered as a substrate lab client (id b9bfe681, act_1801857321221826 allowlisted, standard write budget, enabled, pixel 4240166812964720 alive, page 1093475377188454); Bernard verified via get_status and holds the rebuild spec** (consolidated Purchase campaign, brand-story creative only, Audience Network excluded, retargeting gated on audiences being rebuilt; all PAUSED on dispatch, founder activates). Standing process (ruled 2026-08-05, in Bernard's shared memory): no spec, no dispatch until the client completes the FULL onboarding including the questionnaire. **Gibran COMPLETED onboarding 09:45 UTC 2026-08-05** (questionnaire: $1,500-3,000/mo budget, ROAS 2.5, CPA ~$100+, over-40 affluent USA, purchases; competitor set Santoni/Loro Piana/Morjas/Level Shoes/Allen Edmonds; Slack step SKIPPED, no channel created, founder to nudge or create manually). **Bernard's final spec is written and in his file: $50/day launch scaling to $100 on purchase signal, men 40+ USA with Advantage expansion, brand-story + 21-July PDP statics, no discount creative, no Audience Network, retargeting gated on populating audiences, all PAUSED on dispatch. BUILT 2026-08-05 after six dispatch rounds, verified on Meta by direct read: campaign 120249541099780607 + ad set 120249541100070607 ("AB | Cold | US | Advantage (40+ suggested) | Brand-story + PDP", $40/day ad-set-level budget, OFFSITE_CONVERSIONS on the live pixel, US men, Advantage ON with 40+ as suggestion per founder ruling, Facebook+Instagram only, Audience Network absent). ALL PAUSED; founder activates. The six rounds each consumed one real error: write permission (founder granted Manage campaigns), pixel promoted-object binding, deprecated facebook_video_feeds placement, budget location (ad-set level, not CBO), Advantage-vs-hard-age-floor (founder ruled suggestion mode). WART: rounds 2-5 left four orphan empty campaign shells (…997940607, …949050607, …981320607, …069910607, all PAUSED) because the executor scores an empty batch body on successful campaign creates as failure; deletion = founder hand or approvable fix; the parser bug is spawned as its own work item. Next: ads dispatch after Gibran grants @atelierbrunos IG access (connected in BM, our access pending), then retargeting behind its audience gate.** Month-one framing for Gibran: signal rebuilding, not target hitting (historic CPA $446 vs implied target ~$124; site ATC 1.08% is the co-dependency). Re-audit done 2026-08-05: account dark since 27 July (clean relaunch, not a restructure), pixel alive on organic, retargeting still absent, reusable 21-July PDP statics found; July spec stands with amendments, held pending the questionnaire. Cosmetic: the lab doctrine label renders as bare "v2" (Contract v2, doctrine-is-code; skill fields are Manus-era and unused). Caught minutes before contract generation: plan name and agreement title were hardcoded "Paid Search"; now channel-aware (planNameFor), a Meta client signs a Managed Meta Ads Services Agreement. Slack: workspace is Pro; app has groups:write; client channels create PRIVATE by default (public fallback on missing scope); Gibran joins as a free single-channel guest post-payment. Bernard's Brunos file is complete in the shared store (audit numbers, structural faults, client profile, commercial engagement, IG-correction history). Post-signature runbook: register atelier-brunos as a substrate lab client + allowlist act_1801857321221826, then Bernard specs and dispatches the rebuild from chat, everything created paused. Vercel Skew Protection enabled on both portal projects after stale-tab bites. **Multi-currency live 2026-08-05: clients.currency (migration 0023, applied to BOTH portal DBs), ENTITY_CURRENCIES env drives a selector on the new-client form (FZCO: USD,AED); the chosen currency flows through quote, agreement, native-AED Stripe checkout, welcome email and admin displays. NULL currency = deployment default, so existing clients and single-currency deployments unchanged. Currency is locked per client at creation, deliberately (contract amount = charged amount). Founder ruling 2026-08-05: Stripe Adaptive Pricing stays ON for the FZCO account (foreign-card payers may see a converted local figure alongside the contracted currency; Stripe owns the conversion, settlement unchanged). Wizard auto-advance shipped same day: the contract step polls /api/onboarding/[id]/step every 4s and refreshes itself when the signature webhook lands, ending the reload-after-signing friction.** app.wmiltd.com remains email-silent until it gets its own Resend keys, and its funnel remains entirely unproven (never run once): Resend domain at Bluehost + live Stripe key + live webhook + entity/VAT env review + a full rehearsal are all pending before any wmiltd onboarding link goes out.

Agents on the clone, assessed and part-armed 2026-08-04: same code as wmiltd (the clone auto-deploys main), but per-deployment env and data. Schema drift fixed: migrations 0022 (agent_memory, now IN the app series as of today so future clones get it) and 0021 (platform_client) applied to the FZCO Supabase (project ref vpagppjjdonxqluzglia, pooler aws-0-eu-west-1; local creds in ~/.config/singularweb/fzco.env, a THIRD database, mind the two-DB warning). Remaining to make agents think there: ANTHROPIC_API_KEY in the FZCO Vercel env + redeploy (founder). Oscar gets real when FZCO has Google Ads credentials/accounts; Bernard only if FZCO ever runs Meta. Memory pools: SHARED across deployments as of 2026-08-05, founder-ruled ("one mind, many offices" / "build the shared memory store now before they drift"). Mechanics: `MEMORY_SUPABASE_URL` + `MEMORY_SUPABASE_SECRET_KEY` in `src/lib/agent-memory.ts` route memory reads/writes to one database; the FZCO Vercel project sets them to the wmiltd portal DB values; unset (wmiltd itself) = own DB, unchanged. Conversations stay per-portal; only knowledge is shared. Two-portal capability facts written as shared memories for both agents (Meta reads both portals, substrate dispatch wmiltd-only, Google creds wmiltd-only). Interim drift-copy from earlier on 2026-08-04 (58 rows copied into the FZCO pool) is superseded: once the env vars land, the FZCO-local agent_memory table goes unread; its native rows were context notes now covered by the shared facts, so nothing needs migrating back. PROVEN LIVE 2026-08-05: env vars set, marker memory written via wmiltd Bernard, recalled by FZCO Bernard seconds later, then retired via the forget tool (audit trail kept). One mind, two offices, working.

### Upsells in the portal: built 2026-08-11, both entities

Sell extra work to an existing client from the client record: one-off (setup, ad hoc task) or an ongoing monthly add-on. One client-facing link covers both, so the client is never asked which flow they are in. `/upsell/[id]` is public, like `/onboarding/[id]`, and outside the `(admin)` route group that carries the Auth0 gate.

**Two founder rulings from 2026-08-11 are load-bearing in the design, not preferences.**

**A recurring upsell bills as its OWN Stripe subscription, never as an added line on the retainer.** The founder's reason is better than the one originally proposed: cancellation isolation. A client must be able to drop an add-on with no route by which that action could reach the core retainer, and a single subscription carrying both makes that impossible to offer safely. Consequence in the schema: `stripe_subscription_id` lives per upsell, not on the client.

**A recurring upsell needs a signable quote before payment; a one-off does not, because the invoice is the record.** That asymmetry is the only real branch in `src/lib/upsells.ts`. It reuses the existing `contracts` abstraction, so FZCO signs through the proposal engine and wmiltd through PandaDoc with no per-entity code. Signature is detected by polling the provider on page load, the same way onboarding does it, so neither provider needs a webhook.

Hardened deliberately, because both failures have already happened in this codebase: settlement verifies the session with Stripe rather than trusting the return URL, and the status update carries an `.in([...])` guard so a concurrent webhook cannot double-settle or double-log. One-offs enable Stripe invoice creation, since a business booking a cost needs an invoice rather than a receipt.

Migration `0024_upsells.sql` applied to BOTH databases and verified by querying every column the app writes (FZCO via psql, wmiltd portal via REST). RLS is on with no policies, deny-all to anon; every code path uses the service-role client, which bypasses it. Not yet proven from the outside: there is no anon key on this machine to attempt an unauthorised read with.

**Never used on a live client yet.** The recommendation given: put Vincent's current one-off through the Stripe dashboard directly and use the portal for the next one, so the first live run of new billing code is not also the first time it charges a real client.

### Conversational agent runtime: PARKED, verified 2026-08-05 (it was absent from this file, which is how the strategy surface lost sight of it)

All six workflows (OP_conv_agent_runtime, AGENT_webchat, AGENT_postpass, CAP_rag_retrieve, MAINT_kb_ingest, MAINT_agent_weekly_ledger) remain ACTIVE in n8n. **CORRECTION 2026-08-05, same evening: an earlier reading of this claimed "every client is mode=shadow so no AI replies reach visitors". THAT WAS WRONG.** The delivery gate in `AGENT_webchat` node "Shadow policy" is `show = (agent.enabled === true) || previewKeyMatches`; **`mode` never enters the decision**, despite the node's own comment describing shadow as visitor-invisible. **Second half of the same lesson, added 2026-08-15 by the korean-cream channel: `mode` does not gate STORAGE either, so "shadow" is misleading on both axes.** Read read-only against the substrate, counts only: `public.conversations` persists a `transcript` jsonb unconditionally (all 24 rows non-empty, every client, every one of them `mode: shadow`), and a CRM write leg fires on some of them (3 of 24 carry `ghl_contact_id` + `ghl_note_id`, across two clients, on both `open` and `handoff_pending` status, so it is not cleanly handoff-gated). Unverified: the exact workflow condition triggering that leg, which needs an `AGENT_webchat` / `AGENT_postpass` read. **Consequence for anything sensitive: there is no configuration that makes this runtime store nothing attributable; that is a code change.** So `enabled: true` means real visitors see real AI answers regardless of mode. Consequence read live: **KST (`enabled: true`) has the widget embedded and LIVE on www.kst-accountants.co.uk** (n8n chat widget, webhook `agent-webchat-v1`, widget token `wct_1fyk…`, greeting "I am KST Accountants virtual AI assistant"), so it WOULD answer real visitors. **Founder-confirmed 2026-08-05: every conversation on record (all 24, every client) is his own testing, three sessions literally named `kst-diag-N`, so no real visitor has ever been answered and no lead was lost. The exposure is prospective, not historical**, and the clinical gate held under his testing (refused tax-strategy advice, declined to invent figures, offered handoff). Data gap worth fixing: nothing distinguishes a test conversation from a real one, so "is anyone using this" cannot be answered without asking the founder. singularweb (`enabled: true`) has NO widget on the site (probed apex and www), so it is live-but-unreachable. dental-mastery (`enabled: false`) shows visitors "This chat is not available right now" while the runtime computes and logs the real answer. Enabled: singularweb + kst true, dental-mastery + shallowford false. Conversations since 1 July: sw 7 (last 10 Jul), kst 7 (last 18 Jul), dm 5 (LAST 2 AUG), shallowford 3 (test-era). Findings needing founder decisions: (1) the dentalmastery.ai widget still captures visitor messages into an unread void (as recently as 2 Aug), remove it or route captures to a read inbox; (2) same decision for KST's embedded widget; (3) MAINT_kb_ingest **FIXED 2026-08-08, see the entry below**; (4) weekly ledger runs green every Monday but nothing demonstrably reads it. Recommended canon entry: PARKED with revival trigger "first client whose deal includes web chat".

**MAINT_kb_ingest: diagnosed and fixed 2026-08-08.** It was not a credentials problem, despite n8n's "Forbidden, perhaps check your credentials?" headline. Drive's real message was underneath: `Quota exceeded for quota metric 'Queries' and limit 'Queries per minute'`. The workflow exported all 65 knowledge-base documents from Google Drive on every run, every two hours, whether or not any had changed, at two requests a second. That burst exhausted the per-minute query quota on the shared consumer project and killed the run. **The proof it was a rate limit and not a bad document: across five consecutive runs it died at file index 13, 18, 15, 15 and 30.** A poisoned file fails at the same index every time.

Two corrections to what this file previously claimed. **The knowledge base was never stale.** The Drive listing was compared against every row's own `last_ingested`: 64 of 65 documents have not been touched since they were ingested, and the one exception (`FAQ`) was modified one minute after its own ingest back in May. **`Offers_and_Pricing.md` was ingested 6 July at 19:45, two minutes after its Drive copy was last modified at 19:43, so the "amended R-g wording never re-ingested" line was wrong.** The cost of the outage was prospective, not historical: it would have silently missed the next real edit, and it emitted twelve failure alerts a day.

The fix moves the cheap check in front of the expensive one. New column `kb_documents.source_modified_at`, backfilled for the 64 documents provably unchanged since ingest and deliberately left NULL for `FAQ` so it re-exports once. New nodes `Load Ingest State` (one query, `executeOnce`) and `Filter Unchanged`, which pass only files whose Drive `modifiedTime` differs from the recorded one. **Steady state is now zero Drive exports per run instead of 65.** The content-hash gate downstream is unchanged and still decides re-embedding; the new gate is earlier and cheaper, not a replacement. `Stamp Unchanged` on the false branch of `Hash Changed?` records the new `modifiedTime` when Drive reports a touch but the text is identical, otherwise such a file would re-export forever. `Export Doc as Text` now retries five times with a five-second backoff and paces at one request a second, because the quota is shared and a 403 there is transient. Verified before deployment against live data: the filter selects exactly one file, and both new SQL statements execute and roll back cleanly. **Proven in production 2026-08-08 08:00 UTC, the first scheduled run after the change: SUCCESS in 4 seconds** (the failing runs never got that far). Node counts read from the execution: 65 files scanned, `Filter Unchanged` passed 1, 1 Drive export, and the hash was identical so it routed down the false branch to `Stamp Unchanged` rather than re-embedding. That is exactly the touched-but-unchanged case the stamp node exists for, exercised on the first run. All 65 rows now carry `source_modified_at`, so the steady state from here is zero Drive exports per run. R16 Growth Leakage: growth_action view + action_log.is_client_visible exist in the substrate; NOTHING in the portal product references them; no record of any manual assessment; recommended canon entry: retired with scaffolding in place.

**Hybrid-model upgrade request processed 2026-08-26.** The GoPoxy channel's requirements list (`docs/AGENT_UPGRADE_REQUEST_hybrid_model.md`) is dispositioned in `docs/AGENT_UPGRADE_RESPONSE_hybrid_model.md`: much already exists (Norbert live in the Meta optimise loop, Oscar's proposal chassis, metering, the reversal-counted graduation anchor), a ten-step build order is proposed, and four founder rulings were put up. One gap is API-unclosable and stays mitigated by process: Meta's personalised-destination setting is UI-only.

**Meta breakdown reporting shipped 2026-08-30 (d8f7dbb), from the Steffen channel's build brief** (`docs/META_REPORTING_BUILD_BRIEF.md`, now marked BUILT with the detail). The three deliverables: the lead-gen branch through the finding detectors (placement, age, geography now rank on cost per lead when an account carries no purchase value, so deep audits stop silently discarding those sections on every lead-gen client), Bernard's `get_breakdowns` tool (structured placement / age-gender / per-ad / creative-by-placement / creative-by-demographic data in chat, campaign-scoped, caller window), and the internal .docx tables at `/api/bernard/breakdowns/[account]`. The brief's §7 class-of-defect rule stands for future work: anything the portal fetches should be reachable by the agent that gets asked about it.

**Weekly Meta Slack drafts upgraded to breakdown depth 2026-08-31 (b82b0da), founder-triggered.** The Monday cron's drafts were still the original headline-metrics format while the Steffen report ran on the new breakdowns surface, and the founder caught the gap the morning after both existed. The cron now appends placement, age and gender, creative (best placement per creative) and video engagement over the same 7-day window, ranked on ROAS or cost per lead by what the account holds, with the per-account full-tables link, and the narrative is barred from ranking on single-digit result counts. Re-run same day: 8 accounts, 5 generated and delivered, 3 dormant skipped, 0 failures. Operational note: the manual cron trigger needs the real CRON_SECRET from `.env.local`; `vercel env pull` returns a stub for Sensitive variables (memory: vercel-sensitive-env-pull-returns-stubs).

**Agent optimise v1.1 (audience exclusions) ruled and shipped 2026-08-26.** The founder approved `audience_exclude` as Bernard's first widened move class and folded it into v1's graduation bar (both one-liners from `docs/BERNARD_OPTIMISE_V1_1_EXCLUSIONS_SPEC.md` §1, now marked RULED in that doc). Built the same day: substrate migration 0009 (op check constraint), both optimise workflows regenerated from `scripts/build-optimise-workflows.py` and redeployed (staging gains Read adset context / Read audiences / Exclusion checks with same-account, not-already-excluded, not-targeted and cap-of-5 gates plus the §4 learning-reset and may-not-match disclosures on the approval item; execute gains the read-merge-write-diff leg where only the added audience id may differ from the pre-write targeting snapshot), portal grammar widened in `src/lib/bernard.ts` and `bernard-agent.ts`. Tests: 29 gates + 11 verify-diff unit tests green (the new `tests/verify-exclusion.test.js` caught a crash-on-dropped-field bug in the diff before deploy); acceptance tests 3/4/5/8 passed live on Steffen Foerster, test 1's round trip staged as move ab39dbe4 pending the founder's per-move approve. Field note for every session: the same Meta custom audience read `delivery_status` 300/count 20 on the batch `?ids=` surface and 200/count 1000 on the single-object surface at the same instant; never assert audience usability from one surface.

**Founder ruled on the upgrade rulings, same day (2026-08-26).** Ruling 2 YES: Merchant Center supplemental-feed writes authorised as a proposal class (feed-layer only, reversible, Norbert-reviewed). Ruling 3 YES: graduation bar N=4 consecutive weeks on GoPoxy, zero hand-pulls, zero corrections, freelancer still on the account as comparison. Ruling 4 OK: the ten-step build order stands as written. Ruling 1 (widening Bernard's move classes) is held pending: the founder asked for the trade-off to be explained before deciding; the explanation is in the response doc's rulings section. He also added a fifth direction, built the same day: **a Norbert page and chat in the portal as the operation's front door**, where everything is discussed before Oscar and Bernard are called and dispatched, and running on the Anthropic API key rather than his Claude plan (all portal agents already bill to `ANTHROPIC_API_KEY`; the chat surface makes that the default route for agent work). Build: `src/lib/integrations/anthropic/norbert-agent.ts` (Claude Fable 5, a different model family from the Sonnet the two agents run, per the optimise spec's reviewer-independence principle), `/api/norbert/chat` (scope `norbert`, relay header `x-norbert-relay-key` / env `NORBERT_RELAY_KEY`), `NorbertChat` component, `/norbert` page, nav entry, relay entry in `scripts/agent-relay.mjs`. Norbert's portal tools: read Bernard's lab status, read Oscar's proposal queue, dispatch written briefs to either agent (the brief carries a machine-prepended header voiding approval language, so a dispatch can never launder an approval into an agent whose gate is the founder's word; the exchange lands in the target agent's own thread), and his own memory under agent scope `norbert`. He deliberately holds NO approve/apply/decide tools. Founder approved same day; merged to main (f853e62) and live in production, relay round trip verified with `NORBERT_RELAY_KEY` in Vercel. **Slack surface added same day (0f98d0c):** `/api/norbert/slack` receives DMs and @Norbert mentions from a dedicated Slack app in the WMILTD workspace (own bot token and signing secret, separate from the client-channel provisioning bot). Signature-verified, allow-listed to the founder's Slack member id (fail closed, because the workspace holds contractors and clients and Norbert dispatches briefs), Slack retries dropped so a run never duplicates, ack under 3s with the turn running in next/server `after()` up to maxDuration 300, reply chunked and posted back in-thread. Slack, portal and relay share the one `norbert` conversation scope. Env: `SLACK_NORBERT_BOT_TOKEN`, `SLACK_NORBERT_SIGNING_SECRET`, `SLACK_NORBERT_ALLOWED_USERS`. **Verified live end-to-end 2026-08-26**: founder DM answered in Slack with full cross-surface context. Setup lessons: the Slack app needs App Home Messages Tab + "allow users to send messages" ticked or the DM has no composer; Socket Mode must be OFF for the HTTP Request URL; and the value most easily pasted wrong is the Signing Secret (the App ID is also on Basic Information and is 11 chars; the secret is 32 hex chars behind a Show button). The Vercel CLI on this Mac is authed as antoinem78, so env vars and redeploys can be driven from here with `--project app-wmi --scope antoinem78s-projects`.

## 4. Lead generation track

**⚑ Per-client channel files now carry the LIVING state (2026-08-15, lead-gen half of the split): `docs/clients/<slug>.md`.** A client session reads §1, §2, §7 plus its own file, and owns that file. What remains below is track-wide: the WhatsApp bridge product line, the cross-client call-tracking map, and the Twilio estate.

**Channel index (one session owns one file; see §7 for the convention):**

- [KST Accountants](clients/kst.md): the blueprint; call tracking complete and site swapped; nurture written, unbuilt, client unresponsive
- [VIP Accounting](clients/vip-accounting.md): blueprint run 1; knowledge base ingested 2026-08-15; nurture written and ready to build; call tracking is CTM, not ours
- [DentalMastery.ai](clients/dental-mastery.md): B2B, runs inside the WMI UK ad account; no Google measurement on the site at all
- [Shallowford Smiles](clients/shallowford-smiles.md): **channel file is thin and provisional**; the real history is in long sessions on another machine and should overwrite it wholesale
- [Steffen Foerster](clients/steffen-foerster.md): Galapagos workshops, $16,900 ticket; GCLID capture proven on the Calendly leg 2026-08-12, Stripe deposit leg still never fired; all campaigns paused
- [Buggy Trip Marrakech](clients/buggy-trip.md): first waiting client for the WhatsApp bridge; proposal with Baptiste, unsent; auto-tagging unconfirmed and it gates everything
- [WMI's own site and demo estate](clients/wmi-website.md): where products get proven first; WhatsApp widget live, one synthetic click ever
- [Fly-Rides](clients/fly-rides.md): **first HubSpot client**; Scott Good runs his own Google Ads (7345621720, under AM MCC J, ~$4.3k/30d); founder-owned free remediation of the HubSpot to Google Ads revenue loop, checkpoint week of 22 Sept; blueprint at `docs/HUBSPOT_CLIENT_BLUEPRINT.md`

Client history that predates the split lives in git (checkpoint d71cf65) and in the channel files.

### WhatsApp bridge: PROVEN IN PRODUCTION 2026-08-06, and one headline claim disproven

**It works.** Live on wmiltd.com: ad-tagged visit, widget, WhatsApp message, and the contact arrived in GHL carrying `gclid: CLEAN_TEST_789`, thirteen seconds end to end. Chain: widget captures click ids and parks them in `wa_refs` via `RCV_wa_click`; the visitor messages the verified WhatsApp number (+447476925643, connected in GHL as Default); GHL's workflow posts contact fields to `RCV_wa_inbound_wmi`, which FETCHES the message text from GHL's Conversations API (its webhook sends no message body, and its templating would mangle anything invisible anyway), then matches and stamps the click ids onto the contact.

**The disproven claim, stated plainly because it is in documents already sent.** I told Chat, and wrote in Baptiste's brief, that an invisible zero-width ref rides inside the visitor's own words and defeats the delete-the-tracking-text problem. **WhatsApp strips zero-width characters.** Clean test: the prefill was sent unedited and arrived with zero invisible characters. The widget builds the URL correctly (32 encoded characters verified in the browser), so the loss is WhatsApp's normalisation, not our code.

**What actually carries attribution is the time window**: exactly one unclaimed click for that client within 30 minutes wins, and it refuses to guess when several are pending (verified when my own test clicks polluted the window and it correctly declined). `ref_method` records `exact`, `window` or null on every lead, so coverage is measurable rather than assumed. Deterministic at low volume, degrades honestly at high volume. The visible ref (`data-visible-ref="true"`) remains available and does survive, at the cost of being deletable.

**Corrections owed:** the Baptiste technical brief and the Code-to-Chat brief both carry the invisible-ref claim as a differentiator and must be amended before either is used in a pitch. The Marrakech pitch itself was never sent, so it can be written correctly first: the honest differentiator is the server-side click park plus CRM and conversion loop, not invisible text.

### WhatsApp attribution bridge (new product line, founder-initiated 2026-08-03)

**The pitch:** capture WhatsApp leads into GHL with ad-click attribution (gclid/fbclid), track source and completed bookings, attribute back to Google/Meta through the existing OCT spine. First deployment: wmiltd.com. First waiting client: buggytripmarrakech.com (Cloudflare DNS; stack unverified).

**How it works:** the widget captures click ids on-site, parks them in substrate table `wa_refs` (migration 0005, applied) under a short ref (`WA-XXXXXX`) via public webhook `RCV_wa_click` (n8n `tOzFFv8yK99SNDg0`, ACTIVE, tested live: token-validated, allowlisted attribution, silent rejection). The ref rides inside the prefilled wa.me message. Inbound messages hit `RCV_wa_inbound_wmi` (n8n `3gBPGmk4RUQsn27o`, INACTIVE: placeholders), which extracts the ref, marries attribution to the sender's phone, upserts the GHL contact (source whatsapp, tags wa-attributed/wa-organic, custom fields by KEY: google_click_id, fb_click_id, fbc, fbp, landing_page), claims the ref, logs to tasks. Booking attribution then flows through the standard stage-change OCT legs.

**Live now (2026-08-04):** widget served at `https://app.wmiltd.com/wa-widget.js`; wmi tenant tokened; site integration on branch `wa-widget` (renders nothing until `data-number` set). **Both GHL locations are on our Twilio (KST via Warren #6051759, WMI `nyLMzwmEYXnB3MAxFD7K` via Sapna #6051765). WMI PIT minted (`GHL_WMI_PIT` in substrate.env, n8n cred `YZ54SkRAo3Ud5q49`); five custom fields created on the WMI location (Google Click ID, FB Click ID, FBC, FBP, Landing Page); `RCV_wa_inbound_wmi` ACTIVE and proven end to end both ways: ref message -> wa-attributed with gclid stamped, ref-less message -> wa-attributed-window via the 30-minute single-click fallback. Test contacts and refs cleaned. Inbound auth header is the x-bernard-key value (BERNARD_DISPATCH_KEY).** Remaining chain: the 07 (bundle in-review, auto-buy armed) -> founder enables WhatsApp add-on on WMI location + Meta embedded signup with the 07 -> founder builds the GHL workflow (inbound WhatsApp message -> webhook POST to /webhook/wa-inbound-wmi with the header) -> set data-number in the site branch and merge -> live demo -> Marrakech pitch unfreezes.

**Founder ruled 2026-08-03: GHL native WhatsApp, on the 07 number.** Zero Meta API surface on our side; replies happen in the GHL app. Go-live chain, in order: (1) Twilio mobile bundle approves (in-review) → buy the 07 (SMS+voice stock confirmed plentiful); (2) founder enables the WhatsApp add-on on the WMI location (billing = founder-only) and registers the 07 through GHL's Meta embedded signup, verification SMS readable from Twilio Messages by us; (3) founder builds the GHL workflow: inbound WhatsApp message → webhook POST to `/webhook/wa-inbound-wmi` with the x-bernard-key header; (4) founder mints the WMI location PIT into substrate.env → we fill n8n credential `plQQj8NLI6w7xd1X`, swap `WMI_LOCATION_ID_PENDING` in the upsert node, create the five custom fields via API, activate the receiver; (5) set data-number on the site branch `wa-widget`, merge, live test: ad-click visit → widget → WhatsApp message → contact in GHL with gclid. Productization per client = one-line embed + tenant token + receiver clone, same as the CTM pattern; buggytripmarrakech.com queued first.

**Call tracking number map, done 2026-07-31** (`docs/CALL_TRACKING_NUMBER_MAP.md`). KST is clean: one landline everywhere plus Kris's mobile, no existing tracking layer, so a tracking number is a one-edit swap in the site repo. **Shallowford has three distinct 423 numbers** (two on the site, a third in the GHL record), the footprint of a pre-existing tracking or forwarding layer: touch nothing there until a human identifies the real front-desk line. The DM funnel deliberately has no phone. Three founder-side checks remain (GHL Settings then Phone Numbers per location, since the API scope 401s; Google Business Profile numbers; any legacy CallRail or Twilio account), and none of them blocks the WMI agency number purchase.

**Twilio, agency level. Scope ruled 2026-07-31: UK numbers only.** WMI UK does not purchase US numbers; US clients buy their own (CallRail setup for them is a later conversation, not now). Shallowford is out of Twilio scope entirely. Architecture stands: own Twilio account, sub-account per client, GHL `twilioRebilling` on. Legality verified in `~/Documents/TWILIO_UK_FROM_UAE_RESEARCH.md`.

**Twilio credentials live 2026-07-31.** `TWILIO_ACCOUNT_SID`, `TWILIO_API_KEY_SID`, `TWILIO_API_KEY_SECRET` in the substrate env. Account "Web Marketing International", Full (not trial), active, balance about 7 GBP at last read (fine for number rental, will need topping up for real call/SMS volume). One pre-existing US number on the account, +1 646 693 3390 (Manhattan, voice+SMS), billing monthly: founder to decide keep or release, nothing depends on it. No subaccounts exist yet.

**UK Local regulatory bundle SUBMITTED 2026-07-31**, awaiting Twilio review (2 to 3 business days; decision emails antoine.martin@wmiltd.com). SIDs, all in the WMI Twilio account: bundle `BU90cfdc70ae9011d32cbc15e62c5380ab` (regulation `RN61b6f858c1a0b46ae2631346138a283c`, United Kingdom: Local - Business), end-user `ITf8ca4471add97d465e2d56a6ea25802a` (WMI Ltd, CRN 10264568, rep Antoine Martin, antoine.martin@wmiltd.com, +971504468897), supporting document `RD47bedceb1c2651843638f53d87926f32` referencing address `AD8deb9d141c0bab2616e299a72a64108e` (124 City Road, London EC1V 2NX, the registered office, founder-ruled over the older validated Edgware address on file). Watch item: the City Road address record shows `validated=false`; if the review bounces, that is the first suspect.

API quirks learned during submission, so nobody rediscovers them: end-user attribute booleans must be strings (a real boolean 500s), `business_identity` must be literally `DIRECT_CUSTOMER` and `is_subassigned` literally `YES`/`NO`, and a bundle create takes `RegulationSid` OR the country/type trio, never both (error 22202).

**Inventory finding that reshapes the purchase: Twilio's GB geographic stock is voice-only.** Zero SMS-enabled local numbers nationwide at last check, though 020 voice numbers are plentiful. So "call and text UK prospects" means TWO numbers: an 020 geographic for outbound voice CLI (safe from the Ofcom rule that blanks +44 7 CLIs presented from abroad from 15 July 2027), plus a +44 7 mobile for two-way SMS (the Ofcom rule touches voice CLI only, not SMS). The two DRAFT UK Mobile bundles from May 2025 already on the account fit the SMS leg; complete one with the same end-user record rather than starting over.

**Sequence from here, revised 2026-08-03 after the numbers were bought.** The agency turned out to be on **LC Phone**, so there is no agency-level "own Twilio" switch. The supported path is a per-location LC Phone disable via GHL's support form (Location ID + Twilio SID + auth token; processed by support, per location, other locations untouched). **Status 2026-08-03 late evening: KST DONE and PROVEN.** GHL support (Warren, ticket #6051759) switched KST (`Zts49PaUrbGfHuBtpknt`) to our Twilio; founder imported +442046529670 and set forwarding to 020 3150 2074; verified from the Twilio side (voice_url now LeadConnector phone-system inbound, sms_url msgsndr conversations); live test call placed from +442045383367 via the API: completed 23s, logged as a TYPE_CALL conversation in KST's GHL. **KST call tracking COMPLETE 2026-08-06.** Founder-placed bridge call at 15:30 UTC proved the whole path (his mobile 93s, tracked number 90s, forward to the office 85s, audio clear both ways), and the site swap is merged and live: www.kst-accountants.co.uk now publishes 020 4652 9670 on the contact page and lead form. **Finding worth acting on, but not yet on one data point: the office line rang for roughly two minutes at 16:29 on a Thursday and went to voicemail.** For a client whose paid traffic drives calls, unanswered calls are the largest silent leak in the funnel, and from today they are measurable for the first time: every call from the website lands in GHL with its outcome. Plan: measure for two weeks, then take Kris real numbers rather than an anecdote. Interim mitigation is a GHL missed-call notification workflow (founder UI build; workflow creation has no API), so a rung-out lead gets a callback within minutes. Automatic text-back to the caller is NOT available: UK geographic numbers on Twilio are voice-only, and texting from the 07 would show an unfamiliar mobile, which reads as spam for an accountancy firm. **WMI location (`nyLMzwmEYXnB3MAxFD7K`, sub-account "Web Marketing International LTD") still pending with the second agent (Sapna, #6051765)**; when done, import +442045383367 there for the outbound dialer. Blueprint note: every future client location needs this same one-time support form, now a runbook step. **07 PURCHASED 2026-08-05 on bundle approval (pre-authorized): +447476925643 "WMI WhatsApp + SMS (07)", SMS+voice, under the approved Mobile bundle.** WhatsApp signup 2026-08-05: add-on enabled, embedded signup run ("Create a new WhatsApp business account" path), but Meta verification SMS NEVER reaches the Twilio number (shortcode/branded senders cannot deliver to Twilio-hosted numbers; confirmed empty message log, capability sms:true, inbound needs no config). Repeated resends tripped Meta rate limiting (#2494158): COOLDOWN in effect, no further attempts until it clears (try next morning). Next attempt: ONE try, VOICE CALL only, via WhatsApp Manager (business.facebook.com -> WABA -> +447476925643 -> verify -> voice), never SMS. The number answers and records: voice_url -> n8n OP_wa_verify_recorder (bjWLDeec7zw79MEs), transcription on; read the code from Twilio Recordings/Transcriptions. Then back to GHL to finish, data-number into site branch wa-widget, merge, live demo, Marrakech pitch unfreezes. **Callback bridge LIVE and founder-tested 2026-08-04: `scripts/call.mjs` + n8n `OP_call_bridge` (jH7h8lgBLyPhnKsg) ring the founder's mobile from the WMI 020, then bridge to any normal UK number (01/02/03/07 only; premium refused) presenting the 020. No VoIP on the UAE side, so no du/Etisalat exposure. Clean audio confirmed end to end through KST's forwarding. UAE enabled for low-risk outbound dialing only. ~25p/min all-in.** **Web dialer BUILT 2026-08-21, branch `web-dialer` (founder-requested, this reverses the no-VoIP-on-UAE-side stance for his own desk):** `/dial` page in the admin portal, Twilio Voice JS SDK, one leg browser to UK target presenting the 020, so roughly 2p/min instead of ~30p/min. Token route is admin-session gated; TwiML route `/api/twilio/voice` is secret-gated and excluded from the Auth0 proxy matcher; same 01/02/03/07 guard and pinned caller ID server-side. TwiML app AP63616e365cc1e8b59ba224e902a0dba6 already points at production. To go live: add the six TWILIO_* vars from substrate.env to Vercel (ACCOUNT_SID, API_KEY_SID, API_KEY_SECRET, TWIML_APP_SID, VOICE_WEBHOOK_SECRET, CALLER_ID), merge the branch, then a first test call from Dubai decides whether du/Etisalat pass the media path at all. The bridge stays as fallback.

---

## 5. Ecommerce track

**⚑ Per-client channel files now carry the LIVING state (2026-08-15): `docs/clients/instawarm.md`, `docs/clients/gopoxy.md`, `docs/clients/atelier-brunos.md`, `docs/clients/korean-cream.md`.** A client session reads §1, §2, §7 plus its own file, and owns that file. The sections below remain the historical record and the home of clients without channels yet (Monde du Tabouret, Tropical Oasis) plus programme-wide state.

**Channel index (one session owns one file; see §7 for the convention):**

- [instawarm.shop](clients/instawarm.md): connected merchant #1 on the commerce pipeline; $350/mo FZCO retainer + $225 fix package in flight
- [GoPoxy](clients/gopoxy.md): freelancer-managed, analysis-only; Shopify connected; distributor leg reframed to stockists (their GHL already holds a pipeline of enquiries); OCT to be offered free as the retention wedge per the bridge doctrine
- [Atelier Brunos](clients/atelier-brunos.md): Gibran Zaki, AED 1,800/mo via FZCO portal; destination misconfig found and corrected 2026-08-15
- [Super Henry](clients/super-henry.md): prospect (Rhodri); free review in flight; unit-economics rebuild is the lead finding
- [Monde du Tabouret](clients/monde-du-tabouret.md): Phase 0 blocked on client Shopify app setup; monitor fixed 2026-07-30
- [Tropical Oasis](clients/tropical-oasis.md): audit delivered; awaiting client
- [Vasco Electronics](clients/vasco-electronics.md): prospect; FR account audited 2026-08-11, deck delivered
- [House of Isabella UK](clients/house-of-isabella.md): Google only, under Oscar (founder ruling 2026-08-18); 2026-08-18 feed review overturned all three of the client's premises (backorder sold as in stock, titles pulling the SEO field, Shoptimised already live); two ungranted Merchant Centers spending real money
- [The skincare venture](clients/korean-cream.md): founder's own brand; UAE; V1 to V8 verifications open

Client operational history that predates the split lives in git (checkpoint d71cf65) and in the channel files. Below: track-wide lessons and the commerce programme, which stay here per the cross-session agreement.

**A Shopify theme duplicate stages design, not content (learned on instawarm 2026-08-15, applies to every store fix engagement).** Pages, product descriptions, policies, images, metafields and app configuration are store data shared by every theme at once, so they go live on save whichever theme is published. Only theme code and section settings actually stage. Check which side of that line each item of a fix package falls on **before** promising a client a preview link, or the promise can only be kept by building the copy twice, the second time unpaid. Founder ruling on instawarm: one pass on the live store plus a change list, and completion means "live and listed", never "preview approved", because payment on completion must not hang on a client-controlled approval loop.

**Merchant API v1beta is dead as of 28 February 2026; every commerce read must use v1 (found 2026-08-18 on House of Isabella).** `merchantapi.googleapis.com/.../v1beta/...` now returns 409 ABORTED. The working surfaces are `/reports/v1`, `/products/v1` and `/datasources/v1`. Two shapes that cost time and will cost it again on any other merchant: `product_view` rejects a SELECT without `id`, and **raw row counts massively overstate a catalog**, because rows are keyed `{lang}~{feedLabel}~{offerId}` with a parallel `local~` local-inventory set carrying `availability: UNKNOWN`. On HoI that turned "~128k offers", a figure this file and the channel file both carried since 12 August, into a real online catalog of 33,214. **Any offer-count or share figure taken from a Merchant API row count before 2026-08-18 is suspect on every commerce client, not just this one.** Also: including `item_issues` at page size 1000 stalls the request, and Python `urllib` hangs against that host on this machine while curl succeeds. Full mechanics in `docs/clients/house-of-isabella.md` and shared memory.

**Rebuilding an ad set through guided creation silently re-enables Advantage defaults, and one of them changes where the clicks go. Confirmed on Atelier Brunos 2026-08-15; check it on every rebuild.** The 12 August rebuild came back with **personalised destinations on and only "Shop" toggled**, so the website was not a destination at all and every paid click went to the Meta shop. The account kept reporting clicks, add-to-carts and checkouts while the store received nothing: 121 clicks produced 1 landing page view and 132 on-Meta ViewContents, and the client spotted it from his own Shopify sessions before we did. **The setting is UI-side and does not appear in the ad set or creative fields.** From the API the only visible delta was `contextual_multi_ads: OPT_IN` on the rebuilt ads against a fully enumerated all-`OPT_OUT` `degrees_of_freedom_spec` on the ads they replaced; that is a useful smell and the right neighbourhood, but it is not the control, and the answer was one minute in Ads Manager. **Generalisable method from the diagnosis: the same placement behaving differently across two ad sets rules out a placement explanation, and a healthy pixel `last_fired_time` separates "traffic diverted" from "tracking broken".** Second-order lesson: the destination it diverted to renders the client's catalog, which here is 51% out of stock with `item_group_id` empty, so a catalog defect filed under retargeting quietly became the paid landing experience. Full working in `docs/clients/atelier-brunos.md`.

**Male-only targeting on Atelier Brunos prospecting is a founder ruling, not a defect, 2026-08-06.** Ad set 120249561418670607 reads `genders: [1]`. The addressable pool is already several million at men only, so widening buys nothing. Filed with both agents; stop re-flagging it.

**Agent transcript loss, root-caused and fixed 2026-08-06.** Bernard's replies arrived as only their closing paragraph five times in one session. Cause: both agent streams emitted an unconditional `reset` on every tool turn (intended to drop "let me check" preamble), and both the relay client AND the chat route wipe accumulated text on reset. Since the route's accumulator is what gets PERSISTED, every reset also deleted the reply from `agent_conversations`. Bernard and Oscar routinely write the full answer then call `remember`, so the answer was destroyed at exactly the moment it was finished. Fixed in `d3a38f8`: reset fires only for genuine preamble, never on a bookkeeping-only tool turn. Compounding factor to know: `loadConversation` keeps a rolling **40-turn window**, so on a busy day context ages out faster than it appears, which is how an approved spec vanished mid-thread. Second cause of loss, fixed in `f5c1232`: long messages passed as shell arguments get mangled by PowerShell, so `scripts/agent-relay.mjs` now takes `--file <path>` and anything longer than a sentence should use it.

**META_ADS_TOKEN added to the local .env.local, 2026-08-06.** Claude Code sessions on this machine can now do read-only Meta Graph reads directly (GETs only, per the standing ruling). First uses: pulled the five creative ids Bernard's tools could not see, and verified the IG identity baked into the creatives before dispatch. Scratchpad patterns: `get-creatives.mjs`, `get-creative-identity.mjs`. Graph version note: v21 is being auto-bumped, and `instagram_actor_id` is deprecated (error #12); use v23.0 and `instagram_user_id`.

**Patterns this track will need that the lead gen track does not:** catalog and feed health, Advantage+ Shopping, dynamic product retargeting, `Purchase` and value optimisation rather than `Lead`, ROAS and AOV rather than cost per lead.

**Corrected 2026-07-30: catalog diagnostics are available, and they are worth running.** This file previously said diagnostics were unavailable for both Shopify clients because the catalogs were not properly shared or synced. The Atelier Brunos catalog returns a full `diagnostics` payload on request, and that is where the out-of-stock and single-image faults above came from. WMI also holds ADVERTISE and MANAGE on both Monde du Tabouret catalogs. So the read access is there; nobody had asked. The useful read set is `owned_product_catalogs`, then per catalog `external_event_sources`, `product_sets`, `agencies`, `diagnostics`, and a `products` page for `item_group_id`, `availability`, `size`, `color` and the link host. This is the diagnostic worth packaging, and the two clients between them supply both a healthy case and a shell case to test it against.

**Commerce ingestion programme (substrate-wide).** CODE_BRIEF_1b (shape verification) executed 2026-07-30: `~/Documents/REPORT_commerce_substrate_shape_verification_2026-07-30.md`, every item LIVE-VERIFIED. Headlines a fresh session should not rediscover: the live `RCV_ghl_stage_event` is NOT ack-first; the n8n `postgres` role has `rolbypassrls = true` so tenant RLS binds read roles only; no DEV database exists on the conversion plane; OCT Route 1 transits two Make.com webhooks and Route 2's endpoint is this repo's `src/app/api/oct/upload/route.ts`; the live `RCV_form_routing` still carries a raw Shallowford PIT inline (DEV caller cutover still outstanding); n8n is 2.31.5.

**The commissioned strategy session ran 2026-08-12 (this session, on the founder's handoff of both documents): `~/Documents/SINGULARCOMMERCE_STRATEGY_VERDICT_2026-08-12.md`, awaiting founder ruling.** Three verdicts: the closed-loop intelligence BUILD (largely built), the own-brand skincare launch with claims-gated AI consultation PROTOTYPE (the §22 reference implementation, scoped to a Shopify theme + the existing runtime, 3D and generative UI explicitly out), the separate experience platform DEFER (Shopify's 2026 editions shipped Sidekick, agentic storefronts and catalog-into-AI-chats natively, commoditising the generic AI-assistant layer; re-open trigger defined). Highest-ranked new risk: consultation answers about skin concerns may be special-category data under UK/EU GDPR; legal read required before the consultation stores anything attributable. Phase 0 is founder-tier and blocking: product, brand + trademark, entity, market (market picks the claims profile and the registration duties). **Superseded in part 2026-08-12, recorded here 2026-08-15: MARKET AND PRODUCT ARE DECIDED. Market is the UAE (so the consultation data question is UAE PDPL, not GDPR) and the product is one face cream, no SPF, white-labelled from a Korean ODM.** Two governing documents that were not on this file: `~/Downloads/VENTURE_PLAYBOOK_face_cream_UAE.md` and `~/Downloads/SKINCARE_VENTURE_PROJECT_INSTRUCTIONS.md`, both 2026-08-12. Entity is scoped but open (WMI FZCO holds services activities only and needs a trading or e-commerce activity added before any import or sale). Live state and the V1 to V8 verification list are in `docs/clients/korean-cream.md`.

**Origin context, surfaced by the founder 2026-08-12: the commerce programme exists because he intends to launch his own cosmetic brand (premium skincare / face cream).** Founding document: `~/Downloads/SingularCommerce.ai : Strategy Session Brief.md` (2026-07-29, predates all commerce canon): an AI-native commerce experience layer over Shopify, skincare as reference vertical, PoC = 3-5 products with an interactive 3D hero, AI consultation, dynamic storefront. The architecture's `cosmetics_uk` claims profile is that brand's fingerprint. Two unreconciled items: the brief commissioned an Opus 5 strategy session whose required verdict (BUILD/PROTOTYPE/DEFER/REJECT) is not on this machine, and the brief's separate-matrix isolation ruling (own repo/DB for the experience layer) was never reconciled in writing with the capability map's "existing loop, not a second platform" constraint (they answer different questions: map = merchant services, brief = own-brand experience layer). The own brand is the first merchant needing nobody's permission; founder-tier prerequisites are product, brand + trademark, entity, and market (which picks the claims profile).

**Capability map v1 updated to the 2026-08-12 revision and mirrored into the local canon set** (`~/Downloads/SINGULARCOMMERCE_CAPABILITY_MAP_v1.md`): adds supplemental feeds as the standing write mechanism, disapproval/policy management as a continuous operator with the disapproved-share metric, POAS reporting and profit-value bidding (dual-track or founder-approved cutover), and coverage-gap analysis (Baptiste-requested). This file is the commerce doorway's register reference.

**Merchant API fully unlocked 2026-08-12 (evening).** Refresh token re-minted as ceo@singularweb.ai with the scope superset (adwords + datamanager + content; old token stays valid so the deployed portal and OCT are untouched; `scripts/get-google-refresh-token.mjs` now requests all three so a future re-mint can never silently drop one). The Merchant API (successor to the sunset Content API; v1beta died 2026-02-28, use v1) additionally requires one-time GCP-project developer registration against an MC the caller admins: done against Xinzuo's MC 5679526637 after antoinemcc6@gmail.com (the identity holding all client GMC access) granted ceo@ admin + API-developer there. First live reasons read: Xinzuo 314 offers, 1 disapproved (`image_unwanted_overlays`). **Reconciliation finding for the operator design: Ads-side `shopping_product` showed 49 blocked offers vs 1 MC-side disapproval on the same merchant; the surfaces measure campaign-serving eligibility vs MC acceptability and the operator must read both.** Access rulings 2026-08-12: founder granted ceo@ on **HoI main MC 137224936 only** (low-risk account); **Onebed is not managed by us** (the 79% finding is prospect ammo, not a service fix); **FiltersFast deliberately not granted** (avoid raising concerns, retainer protected); HoI secondary MC 5667449015 still ungranted. Report query shape: `reports:search` on `product_view` (id field mandatory in SELECT), page size 1000, ~128 pages for HoI.

**HoI Merchant API sweep, 2026-08-12: the 35.4% ads-side block decomposes into one account-level fix.** 127,860 offers: 52.4% ELIGIBLE, 47.5% ELIGIBLE_LIMITED, 23 hard-disapproved. Drivers: **33,031 offers `missing_shipping_no_shipping_service_defined_for_country` (one MC settings fix lights up a quarter of the catalog)** + same cohort pending initial review for that destination; 60,552 `local_stores_lack_inventory` (local destination only, does not block Shopping); 141 landing_page_error; 109 price_mismatch; ~150-offer policy tail (legal_restrictions 113, sexual_interests 17, personal_hardships 11, restricted_nfs 10, healthcare claims 1: the pre-suspension class). Two-surface rule re-confirmed: MC hard-disapproval 0.02% vs ads-side 35.4% blocked; the operator must read both. Shipping-gap countries identified: **Ireland and Spain** (both ads and free listings). Conversion miscategorisation checked for bidding impact: the miscategorised GA4 actions are primary=false, so **bidding is clean, reporting is inflated ~5x**. **Client report drafted 2026-08-12 on founder request: `~/Documents/HOI_GOOGLE_PRODUCT_DATA_REVIEW_AUG2026.docx`** (for Rick and Chelsea, Anthony's voice, five findings; em-dash and first-person-plural sweeps clean). **Corrected same day on founder review:** campaign geo-targeting was checked against the MC geo finding and every enabled HoI campaign targets the UK only, so the Ireland/Spain suppression costs zero paid traffic today; the finding was reframed as a half-built expansion decision and demoted to third, with the out-of-stock revenue finding (£270,539/90d now dark) promoted to the lead. Lesson recorded in memory as the cross-surface relevance check: no impact claim without joining the finding against the surface where the money moves. Founder sends when ready.

**GMC reads exercised 2026-08-12 (founder item 2, read-only): most of the capability works TODAY via the dev token.** Ads API v24 `shopping_product` exposes per-offer approval status; first live scan of the book: FiltersFast 0.9% disapproved (healthy), Xinzuo 16.8%, **House of Isabella 35.4% (42,669 of 120,373 offers)**, **Onebed 79.1% (178 of 225)**: two evidenced door-openers needing no store access. Disapproval REASONS and all tier-two writes still need the `content` OAuth scope (clean `ACCESS_TOKEN_SCOPE_INSUFFICIENT`; fix = founder re-mints the refresh token with the content scope via `scripts/get-google-refresh-token.mjs`, ten minutes, once). Report: `~/Documents/REPORT_gmc_read_capability_2026-08-12.md`. Join-key design note (founder item 3) delivered alongside: `~/Documents/DESIGN_NOTE_coverage_gap_join_key_2026-08-12.md`; headline: offer-id conventions vary PER OFFER (Xinzuo mixes raw SKUs and `shopify_{CC}_{pid}_{vid}` in one MC), performance-view ids are lowercased, GTIN needs the content scope, and every coverage output carries a visible match-rate header. Nothing beyond reads built; fourteen-day gates unchanged.

**SingularCommerce capability map v1 (Chat, 2026-08-11) reviewed by this session; verification results returned to the founder.** New operational facts it carries: **two merchants are pending access for connection, instawarm.shop and Atelier Brunos**; **FiltersFast's STORE connection is deferred by founder ruling** (too big too soon, $6.5k retainer protected) while their ads-side audit stays the R-c first action. Code flags on the map: the refund-adjustment conversion leg does not exist yet (OCT uploads clicks only, no adjustments service; Meta side additionally gated by the read-only ruling and R15), cohort/replenishment claims need `read_all_orders` + backfill (scope missing from the recipe's four), GMC audits await Content API scope provisioning (deliberate seam in the feed audit), stock reads confirmed to work on `read_products` alone for single-location stores, and the pagination prerequisite is already scoped and settled.

**CODE_BRIEF_2 (Phase A build) executed 2026-07-31.** Report: `~/Documents/REPORT_commerce_phase_a_build_2026-07-31.md`. Recipe: `docs/PROVISIONING_commerce.md`. Live now: migration 0004 (schema `commerce`: events, checkouts, orders, merchant_secrets, plus the RLS-bound `commerce_writer` role per D1a and the `verify_shopify_hmac` SECURITY DEFINER function), `PROC_shopify_event` (active), receiver + Shopify caller + GHL caller templates (inactive), a synthetic-merchant receiver (active) proven against the full §6 battery with signed synthetic payloads (out-of-order, duplicates, forged signature, missing raw body, refund accumulation, checkout completion all correct), and `CAP_commerce_reconcile` (built, comparison logic proven offline, inactive). **Blocked on the founder for Phase B: create the Shopify development store and custom app** (account creation, so his), then webhook registration and the live reconcile loop get their first real proof. Two portable findings: the estate's `set_ctx` CTE does not actually order before the RLS check under a non-bypass role (commerce uses an ordered variant), and n8n 2.31.5 refuses to publish a workflow whose statically referenced sub-workflow is unpublished, so processor activates before receivers.

---

## 6. Deployed, staged, blocked

| Thing | State |
|---|---|
| Meta audit findings engine | **Merged to main 2026-08-11, not yet exercised in production.** `audit-deep.ts` + `meta-findings.ts` (14 detectors) + rewired `meta-generate.ts`. Proven against Vasco FR: 13 findings, reproduced everything found by hand. Next run through Bernard's `run_audit` is the production proof. |
| Vasco FR audit deck | **Delivered 2026-08-11.** `~/Documents/Vasco Translator France - Meta Ads Audit and Strategy Report.pptx`, 43 slides. Exhibits built from account data; real Ads Manager screenshots outstanding (see below). |
| Bernard attachments + memory | **Live on production** (main, 2026-07-30). 20 memories seeded. |
| Oscar (renamed from Ask Rexos) + shared `agent_memory` | **Live on production** (main, 2026-07-30). Migration 0003 applied and verified. |
| Oscar audit document via chat | **Live on production** (main, 2026-07-30). Reuses the existing generator; only the chat route to it was missing. |
| `CAP_commerce_reconcile` multi-client bug | **Found and FIXED 2026-08-15 (instawarm channel, founder-approved live-workflow change).** It had been reconciling only ONE enabled client per run: `Prep call` was a run-once-for-all-items Code node and every node after it used `$('Commerce clients').first()`, so the shop called, the counts compared and the client id logged were all the first row of an unordered query. A skipped client logs nothing, so a missed day was invisible (12-14 Aug logged only `zz-commerce-dev`, 15 Aug only `instawarm`). Now ordered, per-item throughout, with the sub-workflow call in per-item mode so each client reaches its own store. **Proven by a live run logging both clients with different per-store answers.** Detail and the pre-fix backup path in `docs/clients/instawarm.md`. Any fourteen-day gate is now trustworthy with multiple stores connected. |
| `growth_action` view, read-only role | Applied (migration 0001) |
| DM.ai campaigns | Live and spending |
| KST sending domain | Live and verified |
| KST nurture sequence | Written, not built. Blocked on a booking page. |
| KST GHL snapshot | Not started. Blocks the blueprint purpose. |
| MDT Phase 1 | Shells PAUSED. Blocked on client Shopify app setup. |
| MDT Phase 0 monitor | **Fixed and live** (2026-07-30, founder-approved). Gate is now pixel + 20-product floor + pixel linked as event source + no failed checks. Posts on state change, not green only. Manus node removed. |
| Atelier Brunos prospecting | **LIVE on INITIATE_CHECKOUT** (ad set 120249780355700607, $40/day, created 2026-08-12; the ATC ad set 120249561418670607 is now PAUSED). **⚠ CAUSE FOUND 2026-08-15: personalised destinations was on with only "Shop" toggled, so since 13 Aug every paid click went to the Meta shop and not to atelierbrunos.com** (121 clicks, 1 landing page view, 132 on-Meta ViewContents, 0 purchases). Picked up from guided-creation defaults in the 12 Aug rebuild. Client spotted it first. Fix is a founder toggle and will reset learning a third time.** Detail in `docs/clients/atelier-brunos.md`. |
| Atelier Brunos retargeting | **Campaign PAUSED but the ad set is ACTIVE at $21/day** (campaign 120249561479620607, ad set 120249561480450607, WT 180 Days ex PUR 730). The two defective originals are deleted. **The 2026-08-06 pre-activation gate is still unmet: `advantage_audience` is still 1 and there is no frequency cap.** Unpausing in this state ships the fault the audit named. |
| Atelier Brunos | **ENGAGEMENT CLOSED 2026-08-21, founder-ruled.** Ads paused, August fee refunded less the Stripe charge (~AED 1,730-1,750; cancel `sub_1U0zx5DnjEQZvcK8fwtC5vRw` or 5 Sept still bills). **What settles it: the destination fault was fixed 15 Aug and the six clean days after it ran $193.52 for 192 site visits, 22 add-to-carts, 6 checkouts started and 0 sales.** The media was not the failure; roughly $554 across the whole relaunch produced no purchase. **Method lesson for every session: this channel assumed the account was still broken for six days because a daily pull had a hard-coded end date. Re-read before concluding, and never let a self-set window stand in for today.** **New opportunity live: the client asked the same day about a UAE baby-products store (baby mobiles and nursery wall decor), Google-led, wanting market research. Founder ruled the report FREE, same play as the Super Henry pre-commitment review, so do not quote a fee; five-question scope with a go/no-go inside a week, and everything operational is the paid phase after. Research before media is this account's whole lesson. Google Ads credentials do not yet exist for the FZCO deployment (§3), which does not block the research but does block delivery if it concludes "yes, Google".** |
| Atelier Brunos: reviews invisible | **RETRACTED 2026-08-21.** This session found all 127 reviews rendering at zero height and called it the strongest conversion finding on the account, a cheap fix. **The client then explained they are fake, AI-generated, added by his web developer, and he deliberately suppressed them: two-month-old brand, five real sales.** Acting on the "fix" would have meant advising a US-facing store to publish fake reviews, penalised per violation under the FTC's 2024 rule. **Lesson now in shared memory as `configured-is-not-rendered`: a technical read gives you the state of a thing, never the intent behind it, and "somebody removed it on purpose" is invisible from outside. Ask before calling it a quick win.** What survives: the trust gap is real, and the remedy is earning real proof, not displaying what was to hand. Also still true and worth carrying: the July audit credited "127 five-star reviews" from store data, so they were counted and never seen. |
| Atelier Brunos store diagnosis | **Storefront walked 2026-08-15: the store is correctly configured for US buyers** and the structural-checkout theory is dead. $250 USD, US market enabled, checkout loads with card plus Shop Pay and PayPal, **US shipping free via UPS Ground at three days**. Remaining candidates are the Meta in-app browser on mobile (where nearly all delivery actually lands), the final payment authorisation, and Android specifically. Monday checklist in the channel file; the test order is the item that could end it. **Method lesson: a 422 from a hand-built query string is a claim about your own URL until proven otherwise** (a bad `URLSearchParams` build returned "Country/region not supported" and looked like the answer). |
| Atelier Brunos: keep or refund | **Decision held to the store review (2026-08-17).** Bernard verified live 2026-08-15: 30 days = $929.20 spend, 267 site add-to-carts, **48 initiate-checkouts, 1 purchase**; media metrics all improved (CPM $91.52→$45.40, CPC $1.74→$0.90, CTR >5%) so he says nothing meaningful is left on the media side and the failure is downstream. Ruling: hold spend, do not bill another cycle, decide after the checkout review. Refinement from this session: the failure is **not** Android-specific as he proposed, since 48 checkouts across all devices yielded 1 sale, so Monday tests the payment step for all traffic. |
| AB pixel audiences (2 new) | **Created 2026-08-05** (Product Viewers 30d `120249561187070607`, ATC 180d `120249561212070607`), both delivery_status 200. Earlier "not created" entry was wrong. |
| AB substrate write limits | Confirmed 2026-08-06: substrate cannot write custom audiences or ad set mutations. Those are founder-by-hand in Ads Manager. Campaigns, ad sets and ads it can build. |
| AB catalog `item_group_id` | Empty on all 220 products, and 51% out of stock. Client-side Shopify fix, blocks effective dynamic retargeting. |
| `RCV_manus_events` | **Retired** (2026-07-31, founder instruction). Deactivated, renamed `_ARCHIVED_`, backups in `~/Downloads/n8n-archive/`. |
| Commerce Phase A (CODE_BRIEF_2 + 3) | **CLOSED** (2026-08-11). Real store connected (`singularweb-commerce-dev.myshopify.com`), all six §6 cases proven through Shopify's real edge, webhook registration exercised, `CAP_commerce_reconcile` ACTIVE (07:30 London) and proven both ways, `merchant_secrets` unreachable from n8n by construction. Reports: `~/Documents/REPORT_commerce_phase_a_closeout_2026-08-11.md`. Key payload finding: 2026-07 order webhooks carry no customer consent or orders_count; checkout consent is the reliable surface. **Phase B prerequisite (ruled by volume check 2026-08-11): caller pagination.** FiltersFast's most conservative single purchase tracker (GA4) shows ~2,400 ad-attributed orders/week, ~10x the 250/page cap, so `get_orders_since` gains page_info cursor pagination with an explicit ceiling alert; counts need no change. **instawarm.shop commercial record, founder-clarified 2026-08-13: TWO engagements, and client book totals count both.** (1) **$350/month retainer, signed via the FZCO portal**: this is the relationship. (2) **$225 one-off first-look fix package**, agreed 2026-08-12 (founder opened at $450, Vincent countered $225 citing his mentor's view that the changes are unnecessary, founder accepted; Stripe invoice sent ~02:03): scoped delivery inside the retainer relationship. **CONNECTED 2026-08-14 (~23:00): instawarm.shop is the first real merchant live on the commerce pipeline.** Store verified from itself: InstaWarm.shop, canonical `kgh1am-nd.myshopify.com`, USD, America/Chicago, **baseline 5 lifetime orders, 2 products** (frozen pre-fixes). Six webhooks registered at 2026-07 against `RCV_shopify_instawarm`; signing secret vaulted; caller live with a permanent offline token (n8n credential `Shopify Admin , instawarm`); config carries both workflow ids; signed end-to-end proof passed (200 → event row → state row → task → ingest + state_upserted logs; proof rows cleaned, logs retained). Fourteen-day reconciliation gate starts at the next 07:30 sweep. **Connection-path lessons for the recipe (Dev Dashboard replaced admin custom apps):** app created in Dev Dashboard under the founder's org, custom distribution to the store, "Use legacy install flow" must be OFF (managed install; legacy = app-server OAuth and bounces to the App URL), "Embed in admin" OFF, App URL placeholder is fine; the token for an EXTERNAL store comes via one-time authorization-code OAuth with a `127.0.0.1` redirect URL registered on the app (the client-credentials grant returns `shop_not_permitted` outside the app's own org; the store admin shows NO token section for this app type). Client secret doubles as the webhook signing secret.

**Pre-provisioning record 2026-08-13:** store is `kgh1am-nd.myshopify.com` (read off the public storefront), substrate client row `instawarm` (f6a6c751-93ec-47c2-889b-d6d6d068b6a2) with `config.commerce`, receiver `RCV_shopify_instawarm` (bdzhaoOQUz1P9uqC) ACTIVE on an unguessable path, caller `SHOPIFY_admin_caller_instawarm` (g5GRju7akS7TFhyS) staged with placeholder credential, caller id deliberately nulled in config until the token is bound so the daily reconcile logs `skipped_no_caller` instead of alerting on a dead credential. **Remaining: ~10 founder-minutes logged into the store admin (admin.shopify.com/store/kgh1am-nd) to create the custom app** (five read scopes incl. `read_all_orders`), then Code binds token + signing secret, registers the six webhooks, freezes the baseline and runs the end-to-end proof. **Fix-package terms updated 2026-08-14: payment on completion (founder agreed to Vincent's ask), delivery staged on a DUPLICATE THEME with Vincent approving a preview before publish** (matches clone-first anyway). Answers in: power bank INCLUDED; honest shipping = 3-5 day dispatch + US 10-20 business days (CA 12-25, AU/EU 10-20, world 15-30); identity = Vincent Weimer, Minnesota. Still owed: return-policy choice (one word), the About "why" line, domain-hosting location for the branded email, and a new size-chart question (the cm figures read as garment-flat measurements, not body circumference; must be clarified before fit guidance ships). **Copy pack drafted for founder review: `~/Documents/INSTAWARM_FIX_PACK_v1.md`** (exact copy for all ten items, return policy in two variants, execution sequencing per the staged agreement). Delivery model agreed internally: fixes are scoped one-off work in his Shopify admin driven by Code via the browser with the founder logged in (per capability map tier 4; NOT through the read-only connection, which measures). Four factual questions sit with Vincent (power bank inclusion, honest shipping times, which return policy, About-page founder lines) plus collaborator access and domain access for the branded email. Plan: connect the read-only pipeline BEFORE fixes to freeze the baseline (836 product views, 7 add-to-carts since 11 July), then execute, then his test order flows through as first real-merchant proof. Store has zero purchases, so the fourteen-day gate proves agreement, not volume.

**First-merchant search, state as at 2026-08-11 evening.** Xinzuo Australia was named and verified (Shopify, ~67 ad-attributed orders/week, MC 5679526637 linked) but **withdrawn the same day: founder reports access has expired**. Gibran and instawarm.shop have **no orders**, so neither can prove a revenue-truth loop yet. The generic client ask survives as `~/Documents/XINZUO_STORE_CONNECTION_STEPS.md` (five read-only scopes including `read_all_orders`); it is client-agnostic apart from the filename. **Candidate shortlist from data we hold (ad book volume + public platform check, 2026-08-11):** House of Isabella UK is the standout: Shopify confirmed, real purchases ~405-568/30d (~95-130/week, AOV ~£360-400), and a door-opener finding that their Google Ads conversion hygiene is broken (a GA4 add_to_cart action with ~2,969 conversions/30d and an add_payment_info action are both categorised as PURCHASE, inflating any purchase-category reporting ~5x). Onebed.com.au is the second: Shopify confirmed, ~84 orders/30d (~20/week), AOV ~A$764. Estes and Fly-Rides are not Shopify and have negligible volume. Both shortlisted accounts are reporting_only MCC imports; whether the relationship carries a store-connection ask is the founder's call. Provisioning stays ~15 minutes from templates once any merchant's token and secret arrive; the fourteen-day gate starts on connection. Ops note from the same check: Google Ads API v21 was deprecation-blocked mid-check and intermittently returned EMPTY streams rather than errors before blocking outright; the portal pins v24 and is unaffected, but treat empty GAQL results on old versions as untrustworthy, not as absence. |
| Twilio credentials + UK Local bundle | **Submitted for review 2026-07-31** (BU90cfdc70ae9011d32cbc15e62c5380ab). Number purchases follow approval, each on founder confirm. |
| BERNARD_build pre-flight + error capture | **Live in n8n** (2026-07-31), gate hold proven with zero Meta writes |
| Call tracking number map (task 18) | Done (docs/CALL_TRACKING_NUMBER_MAP.md); Twilio scope UK-only |
| Steffen proposal | Delivered, awaiting client |

---

## 7. Working notes for parallel sessions

**Cross-track settlement, agreed with the lead-gen session 2026-08-15 (nine points).** ONE tree for both tracks: flat `docs/clients/<slug>.md` (flat because four running sessions already own these exact paths; a client that accumulates artifacts may gain a `docs/clients/<slug>/` directory alongside, but the `<slug>.md` file stays the entry point). Slugs: the substrate `clients.slug` wins wherever a row exists; where none exists (gopoxy, korean-cream, super-henry, vasco-electronics), the hyphenated slug minted here becomes the substrate slug if a row is ever created. PROJECT_STATE keeps §1, §2, shared infrastructure and the one-line index; index format `- [Name](clients/<slug>.md): one line` (colon, not em dash, per the standing rule). One session owns one client file; two sessions never edit the same file. Cross-client lessons go to shared memory, never into a client file. Substrate-wide programme records (commerce ingestion, Phase A, shape verification) stay in PROJECT_STATE as shared infrastructure. Loose docs in `docs/` stay put, linked from client files. Ecommerce half executed first (this note); lead gen goes second and updates AGENTS.md with the reading order.

**⚑ REQUEST TO THE PLATFORM CHANNEL, founder-directed 2026-08-26: upgrade Oscar and Bernard toward freelancer replacement, Norbert supervising. Full requirements in `docs/AGENT_UPGRADE_REQUEST_hybrid_model.md`** (written by the GoPoxy channel from its live capability-gap ledger). Founder's stated model: he runs ads himself with minimal effort, agents build/optimise/analyse/report behind the propose-approve-apply-verify chassis, Norbert does pre-approval review, post-apply verification, report sign-off and drift watch. Hybrid until a written graduation bar is met, R15.1-style. GoPoxy proposed as the proving ground.

**⚑ HubSpot capability opened 2026-09-03 (Fly-Rides channel).** First client on HubSpot rather than GHL; the founder holds a HubSpot seat and a Zapier seat (both `antoinemcc6@gmail.com`). **Method for reading and writing HubSpot, founder-ruled 2026-09-03: HubSpot's official MCP server, LIVE the same day** (user-scope `hubspot` server, OAuth as the founder's seat, connector "SingularWeb Claude" in the client portal) (`https://mcp.hubspot.com`, OAuth, read and write under the seat's own permissions) for reads and supervised writes, private-app API only if a substrate leg is ever needed, Zapier UI-only by hand. Same write discipline as Google: propose, approve, apply, read back. Blueprint and diagnostic checklist in `docs/HUBSPOT_CLIENT_BLUEPRINT.md`; strategy entry `docs/SINGULARWEB_PROJECT_STATE.md` §6.6.

**Per-client channels (founder-requested 2026-08-15).** The ecommerce track now segments by client: each client gets its own Claude Code session ("channel"), started in this same repo directory, whose first instruction is to read this file's §1, §2 and §7 plus its channel file in `docs/clients/`. Live channel files: `instawarm.md`, `gopoxy.md`, `atelier-brunos.md`, `korean-cream.md`; new ecommerce clients get a new file on the same pattern. The client session OWNS its file and updates it as things land; anything cross-cutting (rulings, platform lessons, entity facts, capability findings) also gets one line here, because this file remains the hub every session reads. **Memory is shared automatically**: all sessions in this directory use one memory store, plus Bernard/Oscar memories via the relay skills. The general/commerce-programme session remains the place for platform work, canon transfer and anything spanning clients.

Both tracks share this repo, the substrate, Supabase, the GHL account and Bernard. So:

- **Coordinate through this file.** Update it when something lands, gets blocked, or a ruling is made. That is the whole mechanism.
- **The strategic layer is `docs/SINGULARWEB_PROJECT_STATE.md`** (repo copy is the master, founder-ruled 2026-09-03; convention in AGENTS.md). Rulings, client-book changes, product-line status, agent capability and closed open items get a section edit plus a §0 changelog line there, in the same commit as the change. Operational state stays here and in the channel files.
- **Agent memory is the other shared surface.** If you learn something durable about an account, it belongs in `agent_memory` (portal DB, scoped by the `agent` column: `bernard` for Meta, `oscar` for Google) so the other session's agent has it too.
- **Watch for collisions on shared files.** `src/lib/integrations/anthropic/`, `src/lib/bernard*.ts` and the substrate workflows are touched by both tracks. Check `git status` and recent commits before editing.
- **Migrations are numbered sequentially** in `docs/substrate-migrations/` (0001 to 0003 taken; **0004 claimed by the ecommerce session 2026-07-31** for `0004_commerce_schema.sql`, target substrate; next free is `0005_`). Two sessions creating the same number will conflict, so claim it here when you start one, and state the target database in the file header per the README.
- Client documents live in `~/Documents/`, not in the repo.
- Credentials are in `~/.config/singularweb/substrate.env` (chmod 600) and `.env.local`. Reference them by variable name; never echo values.
